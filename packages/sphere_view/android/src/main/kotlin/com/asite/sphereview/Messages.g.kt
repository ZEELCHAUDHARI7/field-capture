// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.asite.sphereview

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object MessagesPigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/** Which way a physical camera points. */
enum class PlatformCameraFacing(val raw: Int) {
  BACK(0),
  FRONT(1),
  EXTERNAL(2);

  companion object {
    fun ofRaw(raw: Int): PlatformCameraFacing? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * The device thermal state, normalised across the two platforms'
 * differently-grained scales.
 */
enum class PlatformThermalState(val raw: Int) {
  NOMINAL(0),
  FAIR(1),
  SERIOUS(2),
  CRITICAL(3);

  companion object {
    fun ofRaw(raw: Int): PlatformThermalState? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** How much of the AE/AWB/AF lock the platform actually granted. */
enum class PlatformLockQuality(val raw: Int) {
  FULLY_LOCKED(0),
  BEST_EFFORT(1),
  UNLOCKED(2);

  companion object {
    fun ofRaw(raw: Int): PlatformLockQuality? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Which clock a frame timestamp is expressed on *before* the plugin converts
 * it (§2.5, §3.5). Recorded rather than assumed, because the conversion is
 * different for each and getting it wrong is a silent 10–50 ms pose error.
 */
enum class PlatformTimestampBase(val raw: Int) {
  /**
   * Android `SENSOR_INFO_TIMESTAMP_SOURCE == REALTIME`:
   * `SystemClock.elapsedRealtimeNanos()`, the same base as
   * `SensorEvent.timestamp`. Directly comparable, offset exactly zero.
   */
  ANDROID_REALTIME(0),
  /**
   * Android `SENSOR_INFO_TIMESTAMP_SOURCE == UNKNOWN`: `System.nanoTime()`,
   * which is *not* the sensor base. An offset must be estimated.
   */
  ANDROID_MONOTONIC_UNKNOWN(1),
  /**
   * iOS `CMClockGetHostTimeClock` (`mach_absolute_time`). `CMDeviceMotion`
   * reports `systemUptime`; the plugin measures the offset rather than
   * assuming the two are identical.
   */
  IOS_HOST_TIME(2);

  companion object {
    fun ofRaw(raw: Int): PlatformTimestampBase? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** What the burst actually asks the sensor for. */
enum class PlatformCaptureFormat(val raw: Int) {
  /** Direct JPEG burst. The straightforward path. */
  JPEG(0),
  /**
   * `YUV_420_888` burst with the JPEG encode deferred off the capture
   * thread. R3 §9 found evidence (Nexus 5: ~243 ms JPEG stall vs ~0 ms YUV)
   * that encoding, not sensor readout, dominates per-frame latency — so this
   * is the cheapest way to recover a burst that misses the 600 ms budget,
   * far cheaper than abandoning HDR. Android only.
   */
  YUV_DEFERRED_JPEG(1);

  companion object {
    fun ofRaw(raw: Int): PlatformCaptureFormat? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * How the bracket was actually produced, so a degraded path is recorded
 * rather than mistaken for the real thing (architecture §8).
 */
enum class PlatformBracketMode(val raw: Int) {
  /** The intended path: explicit per-request exposure time with AE off. */
  MANUAL_EXPOSURE_BURST(0),
  /**
   * No `MANUAL_SENSOR`: `CONTROL_AE_EXPOSURE_COMPENSATION` per request. Not a
   * true bracket — AE is still deciding — and it must not be read as one.
   */
  AE_COMPENSATION_BURST(1),
  /** iOS `AVCapturePhotoBracketSettings` with manual exposure settings. */
  PHOTO_BRACKET(2),
  /**
   * `maxBracketedCapturePhotoCount` came back below the requested count:
   * sequential single shots with the exposure changed between them. Slower,
   * and the frames are further apart in time.
   */
  SEQUENTIAL_MANUAL(3),
  /** No usable exposure control at all. One frame at the metered lock. */
  SINGLE_SHOT(4);

  companion object {
    fun ofRaw(raw: Int): PlatformBracketMode? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Android `INFO_SUPPORTED_HARDWARE_LEVEL`. Recorded but **not** gated on —
 * R3 §8 is explicit that the gate is `MANUAL_SENSOR` in
 * `REQUEST_AVAILABLE_CAPABILITIES`, because `LIMITED` devices may or may not
 * have it. Both are reported so the findings can show whether the two ever
 * disagree on real fleet hardware.
 */
enum class PlatformHardwareLevel(val raw: Int) {
  LEGACY(0),
  LIMITED(1),
  FULL(2),
  LEVEL3(3),
  EXTERNAL(4),
  UNKNOWN(5);

  companion object {
    fun ofRaw(raw: Int): PlatformHardwareLevel? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Which reference frame a platform attitude quaternion is expressed against.
 *
 * Carried on every sample rather than inferred from `Platform.isAndroid`,
 * because it is the input to the one conversion Phase 07 §2 explicitly says
 * **must not be trusted to derivation** — the doc's own Android derivation
 * produces a determinant −1 matrix, which would mirror the panorama. Naming
 * the source frame in the data is what lets `pose_frame_conversion.dart` hold
 * one tested conversion per frame instead of one per platform guess.
 */
enum class PlatformPoseFrame(val raw: Int) {
  /**
   * Android `TYPE_GAME_ROTATION_VECTOR`: device→world, where the world frame
   * is `X` ≈ east, `Y` ≈ north, **`Z` up**. Gyro + accelerometer, bias- and
   * scale-corrected by the OS filter, and — the reason it is used instead of
   * `TYPE_ROTATION_VECTOR` — **no magnetometer**, so rebar, lift motors and
   * steel studs cannot bend the heading (Phase 07 §1, Math §1.1).
   */
  ANDROID_GAME_ROTATION_VECTOR(0),
  /**
   * iOS `CMDeviceMotion.attitude.quaternion` under
   * `.xArbitraryCorrectedZVertical`: device→reference, reference `Z`
   * vertical, `X` an arbitrary horizontal direction, yaw drift-corrected.
   * Chosen over `.xTrueNorthZVertical` for the same magnetometer reason.
   */
  IOS_XARBITRARY_CORRECTED_ZVERTICAL(1);

  companion object {
    fun ofRaw(raw: Int): PlatformPoseFrame? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Which clock a pose sample's timestamp is on, before any conversion.
 *
 * Separate from [PlatformTimestampBase], which describes the *camera*. Phase
 * 07 §6 pitfall 1 is that `SensorEvent.timestamp`'s base "varies by device" —
 * usually `elapsedRealtimeNanos`, but not universally — and Phase 06's
 * `TimestampMapper` already assumed the usual case when it put camera frames
 * on `elapsedRealtimeNanos` and called the offset exactly zero. That
 * assumption is now *measured* on the pose side rather than inherited, because
 * if it is wrong the two clocks silently disagree by however long the device
 * has been asleep.
 */
enum class PlatformPoseClockBase(val raw: Int) {
  /**
   * `SensorEvent.timestamp` is `SystemClock.elapsedRealtimeNanos()`. The
   * documented and near-universal case, and the one Phase 06's camera-side
   * `REALTIME` mapping pairs with exactly.
   */
  ANDROID_ELAPSED_REALTIME(0),
  /**
   * `SensorEvent.timestamp` is `System.nanoTime()` — the device that pitfall 1
   * warns about. The offset is real, device-specific, and measured.
   */
  ANDROID_MONOTONIC_NANO_TIME(1),
  /**
   * `CMDeviceMotion.timestamp`, seconds since boot on `systemUptime` — the
   * same base Phase 06's iOS `TimestampMapper` converts sample buffers onto.
   */
  IOS_SYSTEM_UPTIME(2);

  companion object {
    fun ofRaw(raw: Int): PlatformPoseClockBase? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Pixel dimensions. Not `dart:ui.Size` — see `ImageSize`, which this maps to.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PlatformSize (
  val width: Long,
  val height: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PlatformSize {
      val width = pigeonVar_list[0] as Long
      val height = pigeonVar_list[1] as Long
      return PlatformSize(width, height)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      width,
      height,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PlatformSize
    return MessagesPigeonUtils.deepEquals(this.width, other.width) && MessagesPigeonUtils.deepEquals(this.height, other.height)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.width)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.height)
    return result
  }
  override fun toString(): String {
    return "PlatformSize(width=$width, height=$height)"
  }
}

/**
 * A rectangle in sensor-array coordinates, `[left, top, width, height]`.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PlatformRect (
  val left: Long,
  val top: Long,
  val width: Long,
  val height: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PlatformRect {
      val left = pigeonVar_list[0] as Long
      val top = pigeonVar_list[1] as Long
      val width = pigeonVar_list[2] as Long
      val height = pigeonVar_list[3] as Long
      return PlatformRect(left, top, width, height)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      left,
      top,
      width,
      height,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PlatformRect
    return MessagesPigeonUtils.deepEquals(this.left, other.left) && MessagesPigeonUtils.deepEquals(this.top, other.top) && MessagesPigeonUtils.deepEquals(this.width, other.width) && MessagesPigeonUtils.deepEquals(this.height, other.height)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.left)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.top)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.width)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.height)
    return result
  }
  override fun toString(): String {
    return "PlatformRect(left=$left, top=$top, width=$width, height=$height)"
  }
}

/**
 * One physical camera, with enough capability detail for Dart to choose.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CameraDescriptor (
  val id: String,
  val facing: PlatformCameraFacing,
  /** Still-capture sizes offered, largest first. */
  val availableSizes: List<PlatformSize>,
  /**
   * `LENS_INFO_AVAILABLE_FOCAL_LENGTHS` on Android; the single lens focal on
   * iOS where derivable. Used to exclude the ultra-wide: §2.1 picks the
   * camera whose focal is *not* the shortest.
   */
  val focalLengthsMm: List<Double>,
  /** Whether a real per-frame-controlled bracket is available. */
  val supportsBracketing: Boolean,
  /**
   * iOS `maxBracketedCapturePhotoCount`, queried at runtime after the format
   * is set — R3 §4: it is not a fixed number and has no published per-device
   * table. On Android this is the burst length the session will accept.
   */
  val maxBracketCount: Long,
  /**
   * Whether a lens distortion model can be obtained. Frequently false: R2
   * found `LENS_DISTORTION` null even on Pixel hardware, and iOS only has a
   * LUT on devices that deliver calibration data at all.
   */
  val hasDistortionModel: Boolean,
  val hardwareLevel: PlatformHardwareLevel,
  /**
   * `MANUAL_SENSOR` in `REQUEST_AVAILABLE_CAPABILITIES`. **This** is the
   * gate, not [hardwareLevel] (R3 §8).
   */
  val hasManualSensor: Boolean,
  val isLogicalMultiCamera: Boolean,
  /**
   * Why selection skipped this camera, when it did. §2.1 asks for the
   * ultra-wide to be excluded *and logged*, since a future config may want it.
   */
  val excludedReason: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CameraDescriptor {
      val id = pigeonVar_list[0] as String
      val facing = pigeonVar_list[1] as PlatformCameraFacing
      val availableSizes = pigeonVar_list[2] as List<PlatformSize>
      val focalLengthsMm = pigeonVar_list[3] as List<Double>
      val supportsBracketing = pigeonVar_list[4] as Boolean
      val maxBracketCount = pigeonVar_list[5] as Long
      val hasDistortionModel = pigeonVar_list[6] as Boolean
      val hardwareLevel = pigeonVar_list[7] as PlatformHardwareLevel
      val hasManualSensor = pigeonVar_list[8] as Boolean
      val isLogicalMultiCamera = pigeonVar_list[9] as Boolean
      val excludedReason = pigeonVar_list[10] as String?
      return CameraDescriptor(id, facing, availableSizes, focalLengthsMm, supportsBracketing, maxBracketCount, hasDistortionModel, hardwareLevel, hasManualSensor, isLogicalMultiCamera, excludedReason)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      facing,
      availableSizes,
      focalLengthsMm,
      supportsBracketing,
      maxBracketCount,
      hasDistortionModel,
      hardwareLevel,
      hasManualSensor,
      isLogicalMultiCamera,
      excludedReason,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CameraDescriptor
    return MessagesPigeonUtils.deepEquals(this.id, other.id) && MessagesPigeonUtils.deepEquals(this.facing, other.facing) && MessagesPigeonUtils.deepEquals(this.availableSizes, other.availableSizes) && MessagesPigeonUtils.deepEquals(this.focalLengthsMm, other.focalLengthsMm) && MessagesPigeonUtils.deepEquals(this.supportsBracketing, other.supportsBracketing) && MessagesPigeonUtils.deepEquals(this.maxBracketCount, other.maxBracketCount) && MessagesPigeonUtils.deepEquals(this.hasDistortionModel, other.hasDistortionModel) && MessagesPigeonUtils.deepEquals(this.hardwareLevel, other.hardwareLevel) && MessagesPigeonUtils.deepEquals(this.hasManualSensor, other.hasManualSensor) && MessagesPigeonUtils.deepEquals(this.isLogicalMultiCamera, other.isLogicalMultiCamera) && MessagesPigeonUtils.deepEquals(this.excludedReason, other.excludedReason)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.id)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.facing)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.availableSizes)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.focalLengthsMm)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.supportsBracketing)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.maxBracketCount)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasDistortionModel)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hardwareLevel)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasManualSensor)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.isLogicalMultiCamera)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.excludedReason)
    return result
  }
  override fun toString(): String {
    return "CameraDescriptor(id=$id, facing=$facing, availableSizes=$availableSizes, focalLengthsMm=$focalLengthsMm, supportsBracketing=$supportsBracketing, maxBracketCount=$maxBracketCount, hasDistortionModel=$hasDistortionModel, hardwareLevel=$hardwareLevel, hasManualSensor=$hasManualSensor, isLogicalMultiCamera=$isLogicalMultiCamera, excludedReason=$excludedReason)"
  }
}

/**
 * What Dart asks the platform to configure.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CaptureFormatRequest (
  /** `null` means "pick the largest, subject to [preferFourThree]". */
  val captureSize: PlatformSize? = null,
  /**
   * Prefer a 4:3 output: it matches the sensor's full active array, so no
   * crop factor enters the intrinsics (§2.1, Math §4.1), and it gives the
   * largest vertical FOV, which directly reduces the number of rings.
   */
  val preferFourThree: Boolean,
  /**
   * ~1280. Preview is only used for aiming; a full-resolution preview on a
   * tablet burns battery and thermal headroom the stitch will need (§4).
   */
  val previewTargetWidth: Long,
  val format: PlatformCaptureFormat,
  val jpegQuality: Long,
  /**
   * Compute a centre-crop mean RGB per delivered frame. Off in production;
   * on for the grey-card lock test (§6), which needs a *measurement* rather
   * than an eyeball.
   */
  val computeFrameStatistics: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CaptureFormatRequest {
      val captureSize = pigeonVar_list[0] as PlatformSize?
      val preferFourThree = pigeonVar_list[1] as Boolean
      val previewTargetWidth = pigeonVar_list[2] as Long
      val format = pigeonVar_list[3] as PlatformCaptureFormat
      val jpegQuality = pigeonVar_list[4] as Long
      val computeFrameStatistics = pigeonVar_list[5] as Boolean
      return CaptureFormatRequest(captureSize, preferFourThree, previewTargetWidth, format, jpegQuality, computeFrameStatistics)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      captureSize,
      preferFourThree,
      previewTargetWidth,
      format,
      jpegQuality,
      computeFrameStatistics,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CaptureFormatRequest
    return MessagesPigeonUtils.deepEquals(this.captureSize, other.captureSize) && MessagesPigeonUtils.deepEquals(this.preferFourThree, other.preferFourThree) && MessagesPigeonUtils.deepEquals(this.previewTargetWidth, other.previewTargetWidth) && MessagesPigeonUtils.deepEquals(this.format, other.format) && MessagesPigeonUtils.deepEquals(this.jpegQuality, other.jpegQuality) && MessagesPigeonUtils.deepEquals(this.computeFrameStatistics, other.computeFrameStatistics)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.captureSize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.preferFourThree)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.previewTargetWidth)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.format)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.jpegQuality)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.computeFrameStatistics)
    return result
  }
  override fun toString(): String {
    return "CaptureFormatRequest(captureSize=$captureSize, preferFourThree=$preferFourThree, previewTargetWidth=$previewTargetWidth, format=$format, jpegQuality=$jpegQuality, computeFrameStatistics=$computeFrameStatistics)"
  }
}

/**
 * The raw Android facts Math §4.1 derives intrinsics from. Every field is
 * straight out of `CameraCharacteristics` or the configured stream — no
 * arithmetic happens on the Kotlin side.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class AndroidIntrinsicFacts (
  /** `LENS_INFO_AVAILABLE_FOCAL_LENGTHS[i]` actually in force. */
  val focalLengthMm: Double? = null,
  /** `SENSOR_INFO_PHYSICAL_SIZE`. */
  val sensorPhysicalWidthMm: Double? = null,
  val sensorPhysicalHeightMm: Double? = null,
  /** `SENSOR_INFO_PIXEL_ARRAY_SIZE` — the denominator of the pixel pitch. */
  val pixelArraySize: PlatformSize? = null,
  /**
   * `SENSOR_INFO_PRE_CORRECTION_ACTIVE_ARRAY_SIZE`. Math §4.1 anchors
   * *every* coordinate to this frame.
   */
  val preCorrectionActiveArray: PlatformRect? = null,
  /** `SENSOR_INFO_ACTIVE_ARRAY_SIZE`, for the [activeArraysDiffer] check. */
  val activeArray: PlatformRect? = null,
  /** The `SCALER_CROP_REGION` actually applied. Full array when not zoomed. */
  val cropRegion: PlatformRect? = null,
  /**
   * `LENS_INTRINSIC_CALIBRATION` = `[fx, fy, cx, cy, s]` in
   * pre-correction-array pixels, or null. R2: gated by no capability flag,
   * documented "may be null", reported null even on Pixel hardware — so this
   * is an **override**, not the primary path.
   */
  val lensIntrinsicCalibration: List<Double>? = null,
  /**
   * `LENS_DISTORTION` = `[κ1, κ2, κ3, κ4, κ5]`, **unreordered**. The reorder
   * to OpenCV's `(k1, k2, p1, p2, k3)` happens in Dart, in the one place it
   * already lives.
   */
  val lensDistortion: List<Double>? = null,
  /** Whether `DISTORTION_CORRECTION_MODE_OFF` was requested on every capture. */
  val distortionCorrectionModeOffRequested: Boolean,
  /**
   * Whether this device lists any mode other than `OFF`. R2 §4: most do not,
   * and where none does, the pre-correction/active distinction is moot.
   */
  val distortionCorrectionSupportsNonOff: Boolean,
  /** Whether the two array rectangles actually differ on this device. */
  val activeArraysDiffer: Boolean,
  /** EXIF last resort: `fx = W · FocalLengthIn35mmFilm / 36`. */
  val focalLengthIn35mmFilm: Double? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): AndroidIntrinsicFacts {
      val focalLengthMm = pigeonVar_list[0] as Double?
      val sensorPhysicalWidthMm = pigeonVar_list[1] as Double?
      val sensorPhysicalHeightMm = pigeonVar_list[2] as Double?
      val pixelArraySize = pigeonVar_list[3] as PlatformSize?
      val preCorrectionActiveArray = pigeonVar_list[4] as PlatformRect?
      val activeArray = pigeonVar_list[5] as PlatformRect?
      val cropRegion = pigeonVar_list[6] as PlatformRect?
      val lensIntrinsicCalibration = pigeonVar_list[7] as List<Double>?
      val lensDistortion = pigeonVar_list[8] as List<Double>?
      val distortionCorrectionModeOffRequested = pigeonVar_list[9] as Boolean
      val distortionCorrectionSupportsNonOff = pigeonVar_list[10] as Boolean
      val activeArraysDiffer = pigeonVar_list[11] as Boolean
      val focalLengthIn35mmFilm = pigeonVar_list[12] as Double?
      return AndroidIntrinsicFacts(focalLengthMm, sensorPhysicalWidthMm, sensorPhysicalHeightMm, pixelArraySize, preCorrectionActiveArray, activeArray, cropRegion, lensIntrinsicCalibration, lensDistortion, distortionCorrectionModeOffRequested, distortionCorrectionSupportsNonOff, activeArraysDiffer, focalLengthIn35mmFilm)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      focalLengthMm,
      sensorPhysicalWidthMm,
      sensorPhysicalHeightMm,
      pixelArraySize,
      preCorrectionActiveArray,
      activeArray,
      cropRegion,
      lensIntrinsicCalibration,
      lensDistortion,
      distortionCorrectionModeOffRequested,
      distortionCorrectionSupportsNonOff,
      activeArraysDiffer,
      focalLengthIn35mmFilm,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as AndroidIntrinsicFacts
    return MessagesPigeonUtils.deepEquals(this.focalLengthMm, other.focalLengthMm) && MessagesPigeonUtils.deepEquals(this.sensorPhysicalWidthMm, other.sensorPhysicalWidthMm) && MessagesPigeonUtils.deepEquals(this.sensorPhysicalHeightMm, other.sensorPhysicalHeightMm) && MessagesPigeonUtils.deepEquals(this.pixelArraySize, other.pixelArraySize) && MessagesPigeonUtils.deepEquals(this.preCorrectionActiveArray, other.preCorrectionActiveArray) && MessagesPigeonUtils.deepEquals(this.activeArray, other.activeArray) && MessagesPigeonUtils.deepEquals(this.cropRegion, other.cropRegion) && MessagesPigeonUtils.deepEquals(this.lensIntrinsicCalibration, other.lensIntrinsicCalibration) && MessagesPigeonUtils.deepEquals(this.lensDistortion, other.lensDistortion) && MessagesPigeonUtils.deepEquals(this.distortionCorrectionModeOffRequested, other.distortionCorrectionModeOffRequested) && MessagesPigeonUtils.deepEquals(this.distortionCorrectionSupportsNonOff, other.distortionCorrectionSupportsNonOff) && MessagesPigeonUtils.deepEquals(this.activeArraysDiffer, other.activeArraysDiffer) && MessagesPigeonUtils.deepEquals(this.focalLengthIn35mmFilm, other.focalLengthIn35mmFilm)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.focalLengthMm)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.sensorPhysicalWidthMm)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.sensorPhysicalHeightMm)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.pixelArraySize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.preCorrectionActiveArray)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.activeArray)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.cropRegion)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lensIntrinsicCalibration)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lensDistortion)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.distortionCorrectionModeOffRequested)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.distortionCorrectionSupportsNonOff)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.activeArraysDiffer)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.focalLengthIn35mmFilm)
    return result
  }
  override fun toString(): String {
    return "AndroidIntrinsicFacts(focalLengthMm=$focalLengthMm, sensorPhysicalWidthMm=$sensorPhysicalWidthMm, sensorPhysicalHeightMm=$sensorPhysicalHeightMm, pixelArraySize=$pixelArraySize, preCorrectionActiveArray=$preCorrectionActiveArray, activeArray=$activeArray, cropRegion=$cropRegion, lensIntrinsicCalibration=$lensIntrinsicCalibration, lensDistortion=$lensDistortion, distortionCorrectionModeOffRequested=$distortionCorrectionModeOffRequested, distortionCorrectionSupportsNonOff=$distortionCorrectionSupportsNonOff, activeArraysDiffer=$activeArraysDiffer, focalLengthIn35mmFilm=$focalLengthIn35mmFilm)"
  }
}

/**
 * The raw iOS facts Math §4.2 derives intrinsics from, one field per tier of
 * the chain so the resolver can say which rung it reached.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class IosIntrinsicFacts (
  /**
   * Tier 1: `AVCameraCalibrationData.intrinsicMatrix`, row-major 3×3, valid
   * for [calibrationReferenceSize]. R2: needs a multi-camera virtual device,
   * which excludes base iPad, Air and mini outright — expect null.
   */
  val calibrationIntrinsicMatrix: List<Double>? = null,
  val calibrationReferenceSize: PlatformSize? = null,
  /**
   * Tier 2: `AVCaptureConnection.cameraIntrinsicMatrix`, delivered as a
   * `CMSampleBuffer` attachment on `AVCaptureVideoDataOutput` — *not* photo
   * output. The only measured path on a single-lens iPad, and its
   * availability there is contested (R2 "Still unknown" #1).
   */
  val connectionIntrinsicMatrix: List<Double>? = null,
  val connectionIntrinsicReferenceSize: PlatformSize? = null,
  /** The capability flag says "supported"… */
  val connectionIntrinsicDeliverySupported: Boolean,
  /**
   * …and this says the attachment *actually arrived on real frames*. The
   * distinction is the whole point: reporting the flag alone is the mistake
   * that left this question open since 2017 (Spike B).
   */
  val connectionIntrinsicAttachmentArrived: Boolean,
  /**
   * Tier 3, and the realistic primary: `videoFieldOfView`, confirmed by R2 to
   * be **horizontal**. `fx = (W/2) / tan(FOV/2)`. This is *not*
   * `geometricDistortionCorrectedVideoFieldOfView`, which describes the
   * post-GDC frame and only applies while GDC is on.
   */
  val videoFieldOfViewDegrees: Double,
  /**
   * `lensDistortionLookupTable` — radial magnification factors from
   * [lensDistortionCenterX]/[lensDistortionCenterY] to the farthest corner.
   * Purely radial: the Brown–Conrady fit forces `p1 = p2 = 0`.
   */
  val lensDistortionLookupTable: List<Double>? = null,
  val lensDistortionCenterX: Double? = null,
  val lensDistortionCenterY: Double? = null,
  /**
   * Mandatory on every capture, whichever tier is used (R2 §8). Reported so
   * a violation is visible rather than assumed away.
   */
  val geometricDistortionCorrectionDisabled: Boolean,
  val contentAwareDistortionCorrectionDisabled: Boolean,
  val calibrationDataDeliverySupported: Boolean,
  val isVirtualDevice: Boolean,
  /** EXIF last resort. */
  val focalLengthIn35mmFilm: Double? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): IosIntrinsicFacts {
      val calibrationIntrinsicMatrix = pigeonVar_list[0] as List<Double>?
      val calibrationReferenceSize = pigeonVar_list[1] as PlatformSize?
      val connectionIntrinsicMatrix = pigeonVar_list[2] as List<Double>?
      val connectionIntrinsicReferenceSize = pigeonVar_list[3] as PlatformSize?
      val connectionIntrinsicDeliverySupported = pigeonVar_list[4] as Boolean
      val connectionIntrinsicAttachmentArrived = pigeonVar_list[5] as Boolean
      val videoFieldOfViewDegrees = pigeonVar_list[6] as Double
      val lensDistortionLookupTable = pigeonVar_list[7] as List<Double>?
      val lensDistortionCenterX = pigeonVar_list[8] as Double?
      val lensDistortionCenterY = pigeonVar_list[9] as Double?
      val geometricDistortionCorrectionDisabled = pigeonVar_list[10] as Boolean
      val contentAwareDistortionCorrectionDisabled = pigeonVar_list[11] as Boolean
      val calibrationDataDeliverySupported = pigeonVar_list[12] as Boolean
      val isVirtualDevice = pigeonVar_list[13] as Boolean
      val focalLengthIn35mmFilm = pigeonVar_list[14] as Double?
      return IosIntrinsicFacts(calibrationIntrinsicMatrix, calibrationReferenceSize, connectionIntrinsicMatrix, connectionIntrinsicReferenceSize, connectionIntrinsicDeliverySupported, connectionIntrinsicAttachmentArrived, videoFieldOfViewDegrees, lensDistortionLookupTable, lensDistortionCenterX, lensDistortionCenterY, geometricDistortionCorrectionDisabled, contentAwareDistortionCorrectionDisabled, calibrationDataDeliverySupported, isVirtualDevice, focalLengthIn35mmFilm)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      calibrationIntrinsicMatrix,
      calibrationReferenceSize,
      connectionIntrinsicMatrix,
      connectionIntrinsicReferenceSize,
      connectionIntrinsicDeliverySupported,
      connectionIntrinsicAttachmentArrived,
      videoFieldOfViewDegrees,
      lensDistortionLookupTable,
      lensDistortionCenterX,
      lensDistortionCenterY,
      geometricDistortionCorrectionDisabled,
      contentAwareDistortionCorrectionDisabled,
      calibrationDataDeliverySupported,
      isVirtualDevice,
      focalLengthIn35mmFilm,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as IosIntrinsicFacts
    return MessagesPigeonUtils.deepEquals(this.calibrationIntrinsicMatrix, other.calibrationIntrinsicMatrix) && MessagesPigeonUtils.deepEquals(this.calibrationReferenceSize, other.calibrationReferenceSize) && MessagesPigeonUtils.deepEquals(this.connectionIntrinsicMatrix, other.connectionIntrinsicMatrix) && MessagesPigeonUtils.deepEquals(this.connectionIntrinsicReferenceSize, other.connectionIntrinsicReferenceSize) && MessagesPigeonUtils.deepEquals(this.connectionIntrinsicDeliverySupported, other.connectionIntrinsicDeliverySupported) && MessagesPigeonUtils.deepEquals(this.connectionIntrinsicAttachmentArrived, other.connectionIntrinsicAttachmentArrived) && MessagesPigeonUtils.deepEquals(this.videoFieldOfViewDegrees, other.videoFieldOfViewDegrees) && MessagesPigeonUtils.deepEquals(this.lensDistortionLookupTable, other.lensDistortionLookupTable) && MessagesPigeonUtils.deepEquals(this.lensDistortionCenterX, other.lensDistortionCenterX) && MessagesPigeonUtils.deepEquals(this.lensDistortionCenterY, other.lensDistortionCenterY) && MessagesPigeonUtils.deepEquals(this.geometricDistortionCorrectionDisabled, other.geometricDistortionCorrectionDisabled) && MessagesPigeonUtils.deepEquals(this.contentAwareDistortionCorrectionDisabled, other.contentAwareDistortionCorrectionDisabled) && MessagesPigeonUtils.deepEquals(this.calibrationDataDeliverySupported, other.calibrationDataDeliverySupported) && MessagesPigeonUtils.deepEquals(this.isVirtualDevice, other.isVirtualDevice) && MessagesPigeonUtils.deepEquals(this.focalLengthIn35mmFilm, other.focalLengthIn35mmFilm)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.calibrationIntrinsicMatrix)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.calibrationReferenceSize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.connectionIntrinsicMatrix)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.connectionIntrinsicReferenceSize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.connectionIntrinsicDeliverySupported)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.connectionIntrinsicAttachmentArrived)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.videoFieldOfViewDegrees)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lensDistortionLookupTable)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lensDistortionCenterX)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lensDistortionCenterY)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.geometricDistortionCorrectionDisabled)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.contentAwareDistortionCorrectionDisabled)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.calibrationDataDeliverySupported)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.isVirtualDevice)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.focalLengthIn35mmFilm)
    return result
  }
  override fun toString(): String {
    return "IosIntrinsicFacts(calibrationIntrinsicMatrix=$calibrationIntrinsicMatrix, calibrationReferenceSize=$calibrationReferenceSize, connectionIntrinsicMatrix=$connectionIntrinsicMatrix, connectionIntrinsicReferenceSize=$connectionIntrinsicReferenceSize, connectionIntrinsicDeliverySupported=$connectionIntrinsicDeliverySupported, connectionIntrinsicAttachmentArrived=$connectionIntrinsicAttachmentArrived, videoFieldOfViewDegrees=$videoFieldOfViewDegrees, lensDistortionLookupTable=$lensDistortionLookupTable, lensDistortionCenterX=$lensDistortionCenterX, lensDistortionCenterY=$lensDistortionCenterY, geometricDistortionCorrectionDisabled=$geometricDistortionCorrectionDisabled, contentAwareDistortionCorrectionDisabled=$contentAwareDistortionCorrectionDisabled, calibrationDataDeliverySupported=$calibrationDataDeliverySupported, isVirtualDevice=$isVirtualDevice, focalLengthIn35mmFilm=$focalLengthIn35mmFilm)"
  }
}

/**
 * How frame timestamps were converted to the one monotonic microsecond base
 * the pose stream also uses (§2.5, §3.5).
 *
 * This travels into the bundle. A 10–50 ms mismatch is 0.6–3° of rotation
 * error at a realistic 60°/s pan — larger than everything Phase 03 works to
 * remove, and it presents as a stitcher bug. If it ever goes wrong, this
 * record is how it gets found.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ClockSyncInfo (
  val base: PlatformTimestampBase,
  /**
   * Added to the camera's raw timestamp to land on the sensor/motion clock.
   * Exactly `0` for [PlatformTimestampBase.androidRealtime].
   */
  val offsetUs: Long,
  /**
   * Half the spread of the offset estimator's samples. Zero when the two
   * clocks are the same clock and no estimation was needed.
   */
  val uncertaintyUs: Long,
  val note: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ClockSyncInfo {
      val base = pigeonVar_list[0] as PlatformTimestampBase
      val offsetUs = pigeonVar_list[1] as Long
      val uncertaintyUs = pigeonVar_list[2] as Long
      val note = pigeonVar_list[3] as String
      return ClockSyncInfo(base, offsetUs, uncertaintyUs, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      base,
      offsetUs,
      uncertaintyUs,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ClockSyncInfo
    return MessagesPigeonUtils.deepEquals(this.base, other.base) && MessagesPigeonUtils.deepEquals(this.offsetUs, other.offsetUs) && MessagesPigeonUtils.deepEquals(this.uncertaintyUs, other.uncertaintyUs) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.base)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.offsetUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.uncertaintyUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "ClockSyncInfo(base=$base, offsetUs=$offsetUs, uncertaintyUs=$uncertaintyUs, note=$note)"
  }
}

/**
 * One instantaneous re-measurement of the two clocks, for the §6 stability
 * test ("offset stable within ±2 ms over 60 s").
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ClockOffsetSample (
  val cameraClockUs: Long,
  val motionClockUs: Long,
  val offsetUs: Long,
  val uncertaintyUs: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ClockOffsetSample {
      val cameraClockUs = pigeonVar_list[0] as Long
      val motionClockUs = pigeonVar_list[1] as Long
      val offsetUs = pigeonVar_list[2] as Long
      val uncertaintyUs = pigeonVar_list[3] as Long
      return ClockOffsetSample(cameraClockUs, motionClockUs, offsetUs, uncertaintyUs)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      cameraClockUs,
      motionClockUs,
      offsetUs,
      uncertaintyUs,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ClockOffsetSample
    return MessagesPigeonUtils.deepEquals(this.cameraClockUs, other.cameraClockUs) && MessagesPigeonUtils.deepEquals(this.motionClockUs, other.motionClockUs) && MessagesPigeonUtils.deepEquals(this.offsetUs, other.offsetUs) && MessagesPigeonUtils.deepEquals(this.uncertaintyUs, other.uncertaintyUs)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.cameraClockUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.motionClockUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.offsetUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.uncertaintyUs)
    return result
  }
  override fun toString(): String {
    return "ClockOffsetSample(cameraClockUs=$cameraClockUs, motionClockUs=$motionClockUs, offsetUs=$offsetUs, uncertaintyUs=$uncertaintyUs)"
  }
}

/**
 * The result of opening a camera: everything later stages are only valid for.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CameraOpenResult (
  val captureSize: PlatformSize,
  val previewSize: PlatformSize,
  /**
   * Sensor mounting rotation, needed to reconcile the portrait lock with the
   * sensor's native orientation.
   */
  val sensorOrientationDegrees: Long,
  /**
   * Clockwise degrees the **preview texture still needs** to stand upright on a
   * portrait screen: `0`, `90`, `180` or `270`.
   *
   * The platform states this rather than Dart inferring it, and the difference is
   * not academic — it is the bug that took four attempts to find. Every input to
   * the inference is something the device says about itself: the sensor mounting,
   * the reported preview size, and — the one that actually decided it — whether
   * the render path already applied the rotation. On Android that last answer
   * comes from `SurfaceProducer.handlesCropAndRotation()`, is `true` on the legacy
   * texture path and `false` on the API 29+ `ImageReader` backend, and cannot be
   * derived from anything visible to Dart. A derivation that got the first two
   * right still turned a correct preview sideways.
   *
   * So: `0` when the buffer arrives upright, and the mounting angle when it does
   * not. Dart applies exactly this and nothing else.
   */
  val previewRotationDegrees: Long,
  /**
   * Whether the render path handled the crop and rotation metadata itself.
   *
   * Reported alongside [previewRotationDegrees] rather than folded into it because
   * the two answer different questions — "how far do I turn it" and "why" — and the
   * second is what makes a bug report from an unfamiliar device actionable. It
   * lands in `bundle.json`, so a capture carries the reason with it.
   */
  val previewHandlesRotation: Boolean,
  val clock: ClockSyncInfo,
  /**
   * The bracket path this device will actually take, decided at open time so
   * the caller can adjust the plan rather than discover it at frame 1.
   */
  val bracketMode: PlatformBracketMode,
  val maxBracketCount: Long,
  /**
   * False means a crop factor entered the intrinsics and §2.1's preference
   * could not be honoured. Worth surfacing: it changes the derivation.
   */
  val captureAspectIsFourThree: Boolean,
  val androidFacts: AndroidIntrinsicFacts? = null,
  val iosFacts: IosIntrinsicFacts? = null,
  /** Any compromise made while opening. Never silently degrade (arch §8). */
  val warning: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CameraOpenResult {
      val captureSize = pigeonVar_list[0] as PlatformSize
      val previewSize = pigeonVar_list[1] as PlatformSize
      val sensorOrientationDegrees = pigeonVar_list[2] as Long
      val previewRotationDegrees = pigeonVar_list[3] as Long
      val previewHandlesRotation = pigeonVar_list[4] as Boolean
      val clock = pigeonVar_list[5] as ClockSyncInfo
      val bracketMode = pigeonVar_list[6] as PlatformBracketMode
      val maxBracketCount = pigeonVar_list[7] as Long
      val captureAspectIsFourThree = pigeonVar_list[8] as Boolean
      val androidFacts = pigeonVar_list[9] as AndroidIntrinsicFacts?
      val iosFacts = pigeonVar_list[10] as IosIntrinsicFacts?
      val warning = pigeonVar_list[11] as String?
      return CameraOpenResult(captureSize, previewSize, sensorOrientationDegrees, previewRotationDegrees, previewHandlesRotation, clock, bracketMode, maxBracketCount, captureAspectIsFourThree, androidFacts, iosFacts, warning)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      captureSize,
      previewSize,
      sensorOrientationDegrees,
      previewRotationDegrees,
      previewHandlesRotation,
      clock,
      bracketMode,
      maxBracketCount,
      captureAspectIsFourThree,
      androidFacts,
      iosFacts,
      warning,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CameraOpenResult
    return MessagesPigeonUtils.deepEquals(this.captureSize, other.captureSize) && MessagesPigeonUtils.deepEquals(this.previewSize, other.previewSize) && MessagesPigeonUtils.deepEquals(this.sensorOrientationDegrees, other.sensorOrientationDegrees) && MessagesPigeonUtils.deepEquals(this.previewRotationDegrees, other.previewRotationDegrees) && MessagesPigeonUtils.deepEquals(this.previewHandlesRotation, other.previewHandlesRotation) && MessagesPigeonUtils.deepEquals(this.clock, other.clock) && MessagesPigeonUtils.deepEquals(this.bracketMode, other.bracketMode) && MessagesPigeonUtils.deepEquals(this.maxBracketCount, other.maxBracketCount) && MessagesPigeonUtils.deepEquals(this.captureAspectIsFourThree, other.captureAspectIsFourThree) && MessagesPigeonUtils.deepEquals(this.androidFacts, other.androidFacts) && MessagesPigeonUtils.deepEquals(this.iosFacts, other.iosFacts) && MessagesPigeonUtils.deepEquals(this.warning, other.warning)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.captureSize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.previewSize)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.sensorOrientationDegrees)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.previewRotationDegrees)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.previewHandlesRotation)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.clock)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.bracketMode)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.maxBracketCount)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.captureAspectIsFourThree)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.androidFacts)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.iosFacts)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.warning)
    return result
  }
  override fun toString(): String {
    return "CameraOpenResult(captureSize=$captureSize, previewSize=$previewSize, sensorOrientationDegrees=$sensorOrientationDegrees, previewRotationDegrees=$previewRotationDegrees, previewHandlesRotation=$previewHandlesRotation, clock=$clock, bracketMode=$bracketMode, maxBracketCount=$maxBracketCount, captureAspectIsFourThree=$captureAspectIsFourThree, androidFacts=$androidFacts, iosFacts=$iosFacts, warning=$warning)"
  }
}

/**
 * Who made this device and what it is called.
 *
 * Phase 11 §2 writes these into the output's EXIF `Make`/`Model`. That is not
 * bookkeeping: a construction record is looked at months later by somebody
 * deciding whether to trust it, and "which tablet took this" is the first
 * question when one station's panoramas are visibly worse than the rest of the
 * walk. It is also the only way a field failure can be tied to a device model
 * without asking the person who captured it to remember.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DeviceIdentity (
  /** `Build.MANUFACTURER` on Android; `Apple` on iOS. */
  val make: String,
  /**
   * `Build.MODEL` on Android; the hardware identifier (`iPad14,3`) on iOS.
   *
   * The identifier rather than the marketing name, because the marketing name
   * needs a lookup table that goes stale with every release, and the
   * identifier is what a capability question is actually answered against.
   */
  val model: String,
  /** OS release string, for the bundle's diagnostic record. */
  val osVersion: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DeviceIdentity {
      val make = pigeonVar_list[0] as String
      val model = pigeonVar_list[1] as String
      val osVersion = pigeonVar_list[2] as String
      return DeviceIdentity(make, model, osVersion)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      make,
      model,
      osVersion,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as DeviceIdentity
    return MessagesPigeonUtils.deepEquals(this.make, other.make) && MessagesPigeonUtils.deepEquals(this.model, other.model) && MessagesPigeonUtils.deepEquals(this.osVersion, other.osVersion)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.make)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.model)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.osVersion)
    return result
  }
  override fun toString(): String {
    return "DeviceIdentity(make=$make, model=$model, osVersion=$osVersion)"
  }
}

/**
 * What the metering pre-sweep settled on, and how firmly.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MeteringResult (
  /** The exposure locked for the whole session. */
  val exposureTimeNs: Long,
  val iso: Long,
  val colorTemperatureK: Long,
  /**
   * Locked focus, in diopters. §2.3: near the hyperfocal distance — locking
   * to infinity softens near objects, locking to macro ruins everything else.
   */
  val focusDistanceDiopters: Double,
  val lockQuality: PlatformLockQuality,
  /**
   * How many frames the sweep observed. A sweep that saw three frames did not
   * see the sphere, and its percentile means nothing.
   */
  val sampleCount: Long,
  /** The EV actually locked, relative to the sweep's own median frame. */
  val chosenEv: Double,
  /**
   * Reported alongside [percentile65Ev] so the §2.3 decision is *visible* in
   * the data rather than only in the code: interiors are mid-tone with a few
   * bright windows, and the mean is dragged bright by the windows and crushes
   * the interior.
   */
  val meanEv: Double,
  val percentile65Ev: Double,
  val aeConverged: Boolean,
  /**
   * Whether `NOISE_REDUCTION` / `EDGE` / `TONEMAP` / `COLOR_CORRECTION` were
   * pinned to fixed modes (§2.3 step 5). Easy to miss, and leaving any of
   * them adaptive reintroduces exactly the per-frame photometric
   * inconsistency the AE lock exists to remove.
   */
  val pinnedProcessingModes: Boolean,
  val note: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MeteringResult {
      val exposureTimeNs = pigeonVar_list[0] as Long
      val iso = pigeonVar_list[1] as Long
      val colorTemperatureK = pigeonVar_list[2] as Long
      val focusDistanceDiopters = pigeonVar_list[3] as Double
      val lockQuality = pigeonVar_list[4] as PlatformLockQuality
      val sampleCount = pigeonVar_list[5] as Long
      val chosenEv = pigeonVar_list[6] as Double
      val meanEv = pigeonVar_list[7] as Double
      val percentile65Ev = pigeonVar_list[8] as Double
      val aeConverged = pigeonVar_list[9] as Boolean
      val pinnedProcessingModes = pigeonVar_list[10] as Boolean
      val note = pigeonVar_list[11] as String?
      return MeteringResult(exposureTimeNs, iso, colorTemperatureK, focusDistanceDiopters, lockQuality, sampleCount, chosenEv, meanEv, percentile65Ev, aeConverged, pinnedProcessingModes, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      exposureTimeNs,
      iso,
      colorTemperatureK,
      focusDistanceDiopters,
      lockQuality,
      sampleCount,
      chosenEv,
      meanEv,
      percentile65Ev,
      aeConverged,
      pinnedProcessingModes,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MeteringResult
    return MessagesPigeonUtils.deepEquals(this.exposureTimeNs, other.exposureTimeNs) && MessagesPigeonUtils.deepEquals(this.iso, other.iso) && MessagesPigeonUtils.deepEquals(this.colorTemperatureK, other.colorTemperatureK) && MessagesPigeonUtils.deepEquals(this.focusDistanceDiopters, other.focusDistanceDiopters) && MessagesPigeonUtils.deepEquals(this.lockQuality, other.lockQuality) && MessagesPigeonUtils.deepEquals(this.sampleCount, other.sampleCount) && MessagesPigeonUtils.deepEquals(this.chosenEv, other.chosenEv) && MessagesPigeonUtils.deepEquals(this.meanEv, other.meanEv) && MessagesPigeonUtils.deepEquals(this.percentile65Ev, other.percentile65Ev) && MessagesPigeonUtils.deepEquals(this.aeConverged, other.aeConverged) && MessagesPigeonUtils.deepEquals(this.pinnedProcessingModes, other.pinnedProcessingModes) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.exposureTimeNs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.iso)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.colorTemperatureK)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.focusDistanceDiopters)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.lockQuality)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.sampleCount)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.chosenEv)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.meanEv)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.percentile65Ev)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.aeConverged)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.pinnedProcessingModes)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "MeteringResult(exposureTimeNs=$exposureTimeNs, iso=$iso, colorTemperatureK=$colorTemperatureK, focusDistanceDiopters=$focusDistanceDiopters, lockQuality=$lockQuality, sampleCount=$sampleCount, chosenEv=$chosenEv, meanEv=$meanEv, percentile65Ev=$percentile65Ev, aeConverged=$aeConverged, pinnedProcessingModes=$pinnedProcessingModes, note=$note)"
  }
}

/**
 * One frame returned by a bracketed capture.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PlatformFrame (
  val filePath: String,
  /** The bias in stops this frame was *requested* at. */
  val evBias: Double,
  /**
   * Shutter instant, already converted to the single monotonic microsecond
   * base the pose stream shares (§2.5, §3.5).
   */
  val timestampUs: Long,
  val byteCount: Long,
  /**
   * What the sensor actually did, as opposed to what was asked for. When a
   * bracket comes back with two identical frames — the failure R3 warns about
   * on devices without real bracketing — this is the only evidence of it.
   */
  val exposureTimeNs: Long? = null,
  val iso: Long? = null,
  /**
   * Computed from the *actual* exposure and ISO, so the "EV separation within
   * 0.3 EV of requested" check is against reality rather than intent.
   */
  val achievedEvBias: Double? = null,
  /**
   * Centre-crop channel means, present only when
   * [CaptureFormatRequest.computeFrameStatistics] is set. The grey-card
   * AE/AWB lock measurement (§6).
   */
  val meanR: Double? = null,
  val meanG: Double? = null,
  val meanB: Double? = null,
  val note: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PlatformFrame {
      val filePath = pigeonVar_list[0] as String
      val evBias = pigeonVar_list[1] as Double
      val timestampUs = pigeonVar_list[2] as Long
      val byteCount = pigeonVar_list[3] as Long
      val exposureTimeNs = pigeonVar_list[4] as Long?
      val iso = pigeonVar_list[5] as Long?
      val achievedEvBias = pigeonVar_list[6] as Double?
      val meanR = pigeonVar_list[7] as Double?
      val meanG = pigeonVar_list[8] as Double?
      val meanB = pigeonVar_list[9] as Double?
      val note = pigeonVar_list[10] as String?
      return PlatformFrame(filePath, evBias, timestampUs, byteCount, exposureTimeNs, iso, achievedEvBias, meanR, meanG, meanB, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      filePath,
      evBias,
      timestampUs,
      byteCount,
      exposureTimeNs,
      iso,
      achievedEvBias,
      meanR,
      meanG,
      meanB,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PlatformFrame
    return MessagesPigeonUtils.deepEquals(this.filePath, other.filePath) && MessagesPigeonUtils.deepEquals(this.evBias, other.evBias) && MessagesPigeonUtils.deepEquals(this.timestampUs, other.timestampUs) && MessagesPigeonUtils.deepEquals(this.byteCount, other.byteCount) && MessagesPigeonUtils.deepEquals(this.exposureTimeNs, other.exposureTimeNs) && MessagesPigeonUtils.deepEquals(this.iso, other.iso) && MessagesPigeonUtils.deepEquals(this.achievedEvBias, other.achievedEvBias) && MessagesPigeonUtils.deepEquals(this.meanR, other.meanR) && MessagesPigeonUtils.deepEquals(this.meanG, other.meanG) && MessagesPigeonUtils.deepEquals(this.meanB, other.meanB) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.filePath)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.evBias)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.timestampUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.byteCount)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.exposureTimeNs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.iso)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.achievedEvBias)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.meanR)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.meanG)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.meanB)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "PlatformFrame(filePath=$filePath, evBias=$evBias, timestampUs=$timestampUs, byteCount=$byteCount, exposureTimeNs=$exposureTimeNs, iso=$iso, achievedEvBias=$achievedEvBias, meanR=$meanR, meanG=$meanG, meanB=$meanB, note=$note)"
  }
}

/**
 * One bracket's worth of frames plus the timing the 600 ms budget is judged
 * against.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CaptureResponse (
  val frames: List<PlatformFrame>,
  /**
   * Trigger → last frame's **pixels in hand**, not `onCaptureCompleted`.
   * Metadata routinely completes well before pixels are delivered, and pixels
   * are what Phase 05 needs; timing the metadata would give a flattering,
   * useless number (Spike C).
   */
  val burstWallClockMs: Double,
  /**
   * From sensor timestamps: what the sensor sustains, separated from what
   * processing then costs. That distinction is what the JPEG-vs-YUV decision
   * turns on (R3 §9).
   */
  val shutterToShutterMs: List<Double>,
  val mode: PlatformBracketMode,
  /**
   * Clamping is reported, never silent: a silently clamped bracket looks like
   * a passing capture while delivering less dynamic range than assumed.
   */
  val clampedExposure: Boolean,
  val clampedIso: Boolean,
  /** What the YUV path moved *off* the hot path — not what it removed. */
  val deferredEncodeMs: Double,
  val note: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CaptureResponse {
      val frames = pigeonVar_list[0] as List<PlatformFrame>
      val burstWallClockMs = pigeonVar_list[1] as Double
      val shutterToShutterMs = pigeonVar_list[2] as List<Double>
      val mode = pigeonVar_list[3] as PlatformBracketMode
      val clampedExposure = pigeonVar_list[4] as Boolean
      val clampedIso = pigeonVar_list[5] as Boolean
      val deferredEncodeMs = pigeonVar_list[6] as Double
      val note = pigeonVar_list[7] as String?
      return CaptureResponse(frames, burstWallClockMs, shutterToShutterMs, mode, clampedExposure, clampedIso, deferredEncodeMs, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      frames,
      burstWallClockMs,
      shutterToShutterMs,
      mode,
      clampedExposure,
      clampedIso,
      deferredEncodeMs,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CaptureResponse
    return MessagesPigeonUtils.deepEquals(this.frames, other.frames) && MessagesPigeonUtils.deepEquals(this.burstWallClockMs, other.burstWallClockMs) && MessagesPigeonUtils.deepEquals(this.shutterToShutterMs, other.shutterToShutterMs) && MessagesPigeonUtils.deepEquals(this.mode, other.mode) && MessagesPigeonUtils.deepEquals(this.clampedExposure, other.clampedExposure) && MessagesPigeonUtils.deepEquals(this.clampedIso, other.clampedIso) && MessagesPigeonUtils.deepEquals(this.deferredEncodeMs, other.deferredEncodeMs) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.frames)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.burstWallClockMs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.shutterToShutterMs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.mode)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.clampedExposure)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.clampedIso)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.deferredEncodeMs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "CaptureResponse(frames=$frames, burstWallClockMs=$burstWallClockMs, shutterToShutterMs=$shutterToShutterMs, mode=$mode, clampedExposure=$clampedExposure, clampedIso=$clampedIso, deferredEncodeMs=$deferredEncodeMs, note=$note)"
  }
}

/**
 * What this device can actually do, and — when it cannot — why.
 *
 * Exists so a gyro-less tablet is refused at the feature entry point with a
 * sentence a site manager can act on, rather than producing a panorama built
 * on accelerometer tilt alone (Phase 07 §6 pitfall 3, Phase 12 §1).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PoseCapabilities (
  /**
   * The one hard requirement. Without it there is no attitude source that
   * tracks a pan, and the pipeline cannot work at all.
   */
  val hasGyroscope: Boolean,
  /** Needed for the gravity lock that makes pitch and roll drift-free. */
  val hasAccelerometer: Boolean,
  /**
   * Whether the fused, magnetometer-free attitude sensor is present —
   * `TYPE_GAME_ROTATION_VECTOR` on Android, `isDeviceMotionAvailable` on iOS.
   */
  val hasFusedRotation: Boolean,
  /**
   * Whether a fused gravity vector is available (`TYPE_GRAVITY` /
   * `CMDeviceMotion.gravity`). Its absence is recoverable — raw accelerometer
   * would do — but it is reported because [PlatformPoseSample.upX] is what the
   * levelling step of Math §7 ultimately rests on.
   */
  val hasGravity: Boolean,
  /**
   * **Must be false.** Reported rather than assumed so that a build which
   * accidentally selected `TYPE_ROTATION_VECTOR` or `.xTrueNorthZVertical`
   * shows up as a recorded fact in the bundle instead of as an unexplained
   * heading error on a site with steel in the walls.
   */
  val usesMagnetometer: Boolean,
  /** The frame [PlatformPoseSample]'s quaternion is expressed against. */
  val frame: PlatformPoseFrame,
  /**
   * The fastest the attitude source will run, in µs between samples.
   * `Sensor.getMinDelay()` on Android; 0 where the platform does not say.
   */
  val minDelayUs: Long,
  /**
   * Plain-language description of what was found, for the bundle and the
   * report.
   */
  val detail: String,
  /**
   * Non-null exactly when this device cannot be supported, phrased as
   * something the user can act on.
   */
  val unsupportedReason: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PoseCapabilities {
      val hasGyroscope = pigeonVar_list[0] as Boolean
      val hasAccelerometer = pigeonVar_list[1] as Boolean
      val hasFusedRotation = pigeonVar_list[2] as Boolean
      val hasGravity = pigeonVar_list[3] as Boolean
      val usesMagnetometer = pigeonVar_list[4] as Boolean
      val frame = pigeonVar_list[5] as PlatformPoseFrame
      val minDelayUs = pigeonVar_list[6] as Long
      val detail = pigeonVar_list[7] as String
      val unsupportedReason = pigeonVar_list[8] as String?
      return PoseCapabilities(hasGyroscope, hasAccelerometer, hasFusedRotation, hasGravity, usesMagnetometer, frame, minDelayUs, detail, unsupportedReason)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      hasGyroscope,
      hasAccelerometer,
      hasFusedRotation,
      hasGravity,
      usesMagnetometer,
      frame,
      minDelayUs,
      detail,
      unsupportedReason,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PoseCapabilities
    return MessagesPigeonUtils.deepEquals(this.hasGyroscope, other.hasGyroscope) && MessagesPigeonUtils.deepEquals(this.hasAccelerometer, other.hasAccelerometer) && MessagesPigeonUtils.deepEquals(this.hasFusedRotation, other.hasFusedRotation) && MessagesPigeonUtils.deepEquals(this.hasGravity, other.hasGravity) && MessagesPigeonUtils.deepEquals(this.usesMagnetometer, other.usesMagnetometer) && MessagesPigeonUtils.deepEquals(this.frame, other.frame) && MessagesPigeonUtils.deepEquals(this.minDelayUs, other.minDelayUs) && MessagesPigeonUtils.deepEquals(this.detail, other.detail) && MessagesPigeonUtils.deepEquals(this.unsupportedReason, other.unsupportedReason)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasGyroscope)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasAccelerometer)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasFusedRotation)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.hasGravity)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.usesMagnetometer)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.frame)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.minDelayUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.detail)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.unsupportedReason)
    return result
  }
  override fun toString(): String {
    return "PoseCapabilities(hasGyroscope=$hasGyroscope, hasAccelerometer=$hasAccelerometer, hasFusedRotation=$hasFusedRotation, hasGravity=$hasGravity, usesMagnetometer=$usesMagnetometer, frame=$frame, minDelayUs=$minDelayUs, detail=$detail, unsupportedReason=$unsupportedReason)"
  }
}

/**
 * How pose timestamps were reconciled with the clock Phase 06 puts shutter
 * timestamps on.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PoseClockInfo (
  val base: PlatformPoseClockBase,
  /**
   * Added to a raw sample timestamp to land on the shared motion clock.
   * Exactly `0` for [PlatformPoseClockBase.androidElapsedRealtime] and
   * [PlatformPoseClockBase.iosSystemUptime], where the two *are* one clock.
   */
  val offsetUs: Long,
  /** Bound on the offset estimate. Zero when nothing needed estimating. */
  val uncertaintyUs: Long,
  val note: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PoseClockInfo {
      val base = pigeonVar_list[0] as PlatformPoseClockBase
      val offsetUs = pigeonVar_list[1] as Long
      val uncertaintyUs = pigeonVar_list[2] as Long
      val note = pigeonVar_list[3] as String
      return PoseClockInfo(base, offsetUs, uncertaintyUs, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      base,
      offsetUs,
      uncertaintyUs,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PoseClockInfo
    return MessagesPigeonUtils.deepEquals(this.base, other.base) && MessagesPigeonUtils.deepEquals(this.offsetUs, other.offsetUs) && MessagesPigeonUtils.deepEquals(this.uncertaintyUs, other.uncertaintyUs) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.base)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.offsetUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.uncertaintyUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "PoseClockInfo(base=$base, offsetUs=$offsetUs, uncertaintyUs=$uncertaintyUs, note=$note)"
  }
}

/**
 * What starting the stream settled.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PoseStreamInfo (
  val frame: PlatformPoseFrame,
  val clock: PoseClockInfo,
  /** The period actually requested of the platform (§4 asks for 10 000 µs). */
  val samplingPeriodUs: Long,
  /** The floor the platform imposes on it; 0 when unreported. */
  val minDelayUs: Long,
  val note: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PoseStreamInfo {
      val frame = pigeonVar_list[0] as PlatformPoseFrame
      val clock = pigeonVar_list[1] as PoseClockInfo
      val samplingPeriodUs = pigeonVar_list[2] as Long
      val minDelayUs = pigeonVar_list[3] as Long
      val note = pigeonVar_list[4] as String
      return PoseStreamInfo(frame, clock, samplingPeriodUs, minDelayUs, note)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      frame,
      clock,
      samplingPeriodUs,
      minDelayUs,
      note,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PoseStreamInfo
    return MessagesPigeonUtils.deepEquals(this.frame, other.frame) && MessagesPigeonUtils.deepEquals(this.clock, other.clock) && MessagesPigeonUtils.deepEquals(this.samplingPeriodUs, other.samplingPeriodUs) && MessagesPigeonUtils.deepEquals(this.minDelayUs, other.minDelayUs) && MessagesPigeonUtils.deepEquals(this.note, other.note)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.frame)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.clock)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.samplingPeriodUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.minDelayUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.note)
    return result
  }
  override fun toString(): String {
    return "PoseStreamInfo(frame=$frame, clock=$clock, samplingPeriodUs=$samplingPeriodUs, minDelayUs=$minDelayUs, note=$note)"
  }
}

/**
 * One attitude sample, exactly as the platform reported it.
 *
 * **No frame conversion happens on the native side.** This is the same
 * decision the intrinsics chain makes for the same reason, one step further:
 * Phase 07 §2 says the world→world conversion "must be proven by the §5 test,
 * not by derivation", and a conversion that lives in Kotlin and Swift can only
 * ever be tested on the two devices in the room. In Dart it is one function,
 * pinned by unit tests over every branch, and the device test then confirms
 * the platform docs rather than the arithmetic.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PlatformPoseSample (
  /** Device→reference rotation, in the frame named by [PoseStreamInfo.frame]. */
  val qx: Double,
  val qy: Double,
  val qz: Double,
  val qw: Double,
  /**
   * A **unit** vector in the *device* frame pointing **away from the earth**.
   *
   * Normalised natively rather than passed raw because the two platforms
   * disagree on both magnitude and sign: Android `TYPE_GRAVITY` reads
   * `(0, 0, +9.81)` with the device flat on its back, iOS
   * `CMDeviceMotion.gravity` reads `(0, 0, −1)` in the same pose. Reconciling
   * that is a one-line, per-platform fact about what a sensor reads in a known
   * physical pose — which is precisely the kind of thing the native side is
   * here to report — and it leaves Dart with one convention instead of two.
   *
   * Dart does **not** take it on trust: `PlatformAhrsPoseSource` checks that
   * this vector, rotated into the world frame, actually comes out pointing up,
   * and refuses to start if it does not (Phase 07 §2's reflection hazard, in
   * its detectable half).
   */
  val upX: Double,
  val upY: Double,
  val upZ: Double,
  /**
   * `|ω|` from the gyroscope, rad/s. A magnitude rather than a vector because
   * it is frame-invariant, which keeps one more sign convention out of the
   * wire format — and the steadiness gate only ever wants the magnitude.
   */
  val angularSpeedRadPerSec: Double,
  /**
   * Sample instant on the shared motion clock, already converted by
   * [PoseClockInfo.offsetUs].
   */
  val timestampUs: Long,
  /**
   * Monotonic counter from the start of the stream, so a gap in delivery is
   * distinguishable from a gap in sampling.
   */
  val sequence: Long,
  /**
   * Android `SensorEvent.accuracy` (`SENSOR_STATUS_*`); `-1` on iOS, which
   * publishes no equivalent.
   */
  val accuracy: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PlatformPoseSample {
      val qx = pigeonVar_list[0] as Double
      val qy = pigeonVar_list[1] as Double
      val qz = pigeonVar_list[2] as Double
      val qw = pigeonVar_list[3] as Double
      val upX = pigeonVar_list[4] as Double
      val upY = pigeonVar_list[5] as Double
      val upZ = pigeonVar_list[6] as Double
      val angularSpeedRadPerSec = pigeonVar_list[7] as Double
      val timestampUs = pigeonVar_list[8] as Long
      val sequence = pigeonVar_list[9] as Long
      val accuracy = pigeonVar_list[10] as Long
      return PlatformPoseSample(qx, qy, qz, qw, upX, upY, upZ, angularSpeedRadPerSec, timestampUs, sequence, accuracy)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      qx,
      qy,
      qz,
      qw,
      upX,
      upY,
      upZ,
      angularSpeedRadPerSec,
      timestampUs,
      sequence,
      accuracy,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PlatformPoseSample
    return MessagesPigeonUtils.deepEquals(this.qx, other.qx) && MessagesPigeonUtils.deepEquals(this.qy, other.qy) && MessagesPigeonUtils.deepEquals(this.qz, other.qz) && MessagesPigeonUtils.deepEquals(this.qw, other.qw) && MessagesPigeonUtils.deepEquals(this.upX, other.upX) && MessagesPigeonUtils.deepEquals(this.upY, other.upY) && MessagesPigeonUtils.deepEquals(this.upZ, other.upZ) && MessagesPigeonUtils.deepEquals(this.angularSpeedRadPerSec, other.angularSpeedRadPerSec) && MessagesPigeonUtils.deepEquals(this.timestampUs, other.timestampUs) && MessagesPigeonUtils.deepEquals(this.sequence, other.sequence) && MessagesPigeonUtils.deepEquals(this.accuracy, other.accuracy)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.qx)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.qy)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.qz)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.qw)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.upX)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.upY)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.upZ)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.angularSpeedRadPerSec)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.timestampUs)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.sequence)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.accuracy)
    return result
  }
  override fun toString(): String {
    return "PlatformPoseSample(qx=$qx, qy=$qy, qz=$qz, qw=$qw, upX=$upX, upY=$upY, upZ=$upZ, angularSpeedRadPerSec=$angularSpeedRadPerSec, timestampUs=$timestampUs, sequence=$sequence, accuracy=$accuracy)"
  }
}
private open class MessagesPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformCameraFacing.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformThermalState.ofRaw(it.toInt())
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformLockQuality.ofRaw(it.toInt())
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformTimestampBase.ofRaw(it.toInt())
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformCaptureFormat.ofRaw(it.toInt())
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformBracketMode.ofRaw(it.toInt())
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformHardwareLevel.ofRaw(it.toInt())
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformPoseFrame.ofRaw(it.toInt())
        }
      }
      137.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PlatformPoseClockBase.ofRaw(it.toInt())
        }
      }
      138.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PlatformSize.fromList(it)
        }
      }
      139.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PlatformRect.fromList(it)
        }
      }
      140.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CameraDescriptor.fromList(it)
        }
      }
      141.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CaptureFormatRequest.fromList(it)
        }
      }
      142.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          AndroidIntrinsicFacts.fromList(it)
        }
      }
      143.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          IosIntrinsicFacts.fromList(it)
        }
      }
      144.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ClockSyncInfo.fromList(it)
        }
      }
      145.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ClockOffsetSample.fromList(it)
        }
      }
      146.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CameraOpenResult.fromList(it)
        }
      }
      147.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DeviceIdentity.fromList(it)
        }
      }
      148.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MeteringResult.fromList(it)
        }
      }
      149.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PlatformFrame.fromList(it)
        }
      }
      150.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CaptureResponse.fromList(it)
        }
      }
      151.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PoseCapabilities.fromList(it)
        }
      }
      152.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PoseClockInfo.fromList(it)
        }
      }
      153.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PoseStreamInfo.fromList(it)
        }
      }
      154.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PlatformPoseSample.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is PlatformCameraFacing -> {
        stream.write(129)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformThermalState -> {
        stream.write(130)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformLockQuality -> {
        stream.write(131)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformTimestampBase -> {
        stream.write(132)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformCaptureFormat -> {
        stream.write(133)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformBracketMode -> {
        stream.write(134)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformHardwareLevel -> {
        stream.write(135)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformPoseFrame -> {
        stream.write(136)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformPoseClockBase -> {
        stream.write(137)
        writeValue(stream, value.raw.toLong())
      }
      is PlatformSize -> {
        stream.write(138)
        writeValue(stream, value.toList())
      }
      is PlatformRect -> {
        stream.write(139)
        writeValue(stream, value.toList())
      }
      is CameraDescriptor -> {
        stream.write(140)
        writeValue(stream, value.toList())
      }
      is CaptureFormatRequest -> {
        stream.write(141)
        writeValue(stream, value.toList())
      }
      is AndroidIntrinsicFacts -> {
        stream.write(142)
        writeValue(stream, value.toList())
      }
      is IosIntrinsicFacts -> {
        stream.write(143)
        writeValue(stream, value.toList())
      }
      is ClockSyncInfo -> {
        stream.write(144)
        writeValue(stream, value.toList())
      }
      is ClockOffsetSample -> {
        stream.write(145)
        writeValue(stream, value.toList())
      }
      is CameraOpenResult -> {
        stream.write(146)
        writeValue(stream, value.toList())
      }
      is DeviceIdentity -> {
        stream.write(147)
        writeValue(stream, value.toList())
      }
      is MeteringResult -> {
        stream.write(148)
        writeValue(stream, value.toList())
      }
      is PlatformFrame -> {
        stream.write(149)
        writeValue(stream, value.toList())
      }
      is CaptureResponse -> {
        stream.write(150)
        writeValue(stream, value.toList())
      }
      is PoseCapabilities -> {
        stream.write(151)
        writeValue(stream, value.toList())
      }
      is PoseClockInfo -> {
        stream.write(152)
        writeValue(stream, value.toList())
      }
      is PoseStreamInfo -> {
        stream.write(153)
        writeValue(stream, value.toList())
      }
      is PlatformPoseSample -> {
        stream.write(154)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface SphereCameraHostApi {
  /** Enumerates the physical cameras and their capabilities. */
  fun listCameras(callback: (Result<List<CameraDescriptor>>) -> Unit)
  /** Opens [cameraId] and configures the capture and preview streams. */
  fun open(cameraId: String, format: CaptureFormatRequest, callback: (Result<CameraOpenResult>) -> Unit)
  /**
   * Attaches the preview and returns the Flutter texture id. Preview is
   * delivered through the platform texture APIs; bytes are never streamed
   * over the channel (§4).
   */
  fun attachPreview(callback: (Result<Long>) -> Unit)
  /** Releases the preview texture without closing the camera. */
  fun detachPreview(callback: (Result<Unit>) -> Unit)
  /**
   * Runs the metering pre-sweep for [durationSeconds], then hard-locks AE,
   * AWB and AF for the rest of the session.
   */
  fun meterAndLock(durationSeconds: Double, callback: (Result<MeteringResult>) -> Unit)
  /** Releases the lock. */
  fun unlock(callback: (Result<Unit>) -> Unit)
  /**
   * Fires one bracket at [evBiases], writing frames into [outputDirectory]
   * with names beginning [namePrefix].
   */
  fun captureBracket(evBiases: List<Double>, outputDirectory: String, namePrefix: String, callback: (Result<CaptureResponse>) -> Unit)
  /** Current thermal state (§5). */
  fun thermalState(callback: (Result<PlatformThermalState>) -> Unit)
  /**
   * Re-measures the camera↔motion clock offset. Called repeatedly by the §6
   * stability test; cheap enough to call in a loop.
   */
  fun sampleClockOffset(callback: (Result<ClockOffsetSample>) -> Unit)
  /**
   * Make, model and OS version, for the output's EXIF (Phase 11 §2).
   *
   * On the camera API rather than the pose one because it is asked once, at
   * open time, alongside every other fact about the hardware that took the
   * frames.
   */
  fun deviceIdentity(callback: (Result<DeviceIdentity>) -> Unit)
  /**
   * The largest texture edge this GPU will accept, in pixels (Phase 11 §3.1).
   *
   * The viewer needs this before it uploads a panorama, and it is the one
   * number that decides whether the sphere renders or comes up **black**.
   * Many mid-range tablet GPUs cap at 4096, and an 8192-wide upload against
   * such a cap does not raise an error — it silently produces nothing, which
   * looks exactly like a stitcher that emitted an empty image.
   *
   * Returns 0 when the platform cannot say, which the caller must read as
   * "assume the conservative floor" rather than "no limit".
   */
  fun maxTextureSize(callback: (Result<Long>) -> Unit)
  /** Total physical RAM in MB, for the memory-tier probe (arch §6.5). */
  fun totalPhysicalMemoryMb(callback: (Result<Long>) -> Unit)
  /**
   * How much more memory *this process* may allocate, in MB, or `-1` where
   * the platform will not say.
   *
   * Not the tier decision — that is [totalPhysicalMemoryMb], and it is total
   * rather than available on purpose (Phase 10 §4): available fluctuates, and
   * a tier that moves with it would make output resolution differ between two
   * runs on the same device, which makes a bug report useless.
   *
   * This is the pre-flight check instead. iOS answers it with
   * `os_proc_available_memory()`, and it matters there specifically because
   * iOS kills a process on memory pressure with no recoverable signal — no
   * `bad_alloc` to catch, no chance to drop a tier. So the only defence is to
   * ask before starting, and to start one tier lower if the answer is smaller
   * than the tier needs. Android returns `-1`: it has no per-process
   * equivalent, and a real `OutOfMemoryError` there *is* catchable, so the
   * retry path covers it.
   */
  fun availableProcessMemoryMb(callback: (Result<Long>) -> Unit)
  /**
   * Battery charge as a whole percentage, or `-1` where the platform will not
   * say.
   *
   * Phase 12 §3 asks for the drain **per station** to be measured and
   * published, because a site walk is up to thirty of them and a tablet that
   * cannot finish one is a feature nobody can use. It is a percentage rather
   * than a mAh figure on purpose: percentage is what both platforms report
   * without a vendor API, and the number that matters is "how many stations
   * does this tablet have left", which is a percentage divided by a
   * percentage.
   *
   * It is coarse — most devices report in steps of 1% and some in steps of 5%
   * — so one station's drain is at or below the quantisation. The device matrix
   * therefore measures across a *run* of stations and divides, which is also
   * the number a site lead actually wants.
   */
  fun batteryPercent(callback: (Result<Long>) -> Unit)
  /** Closes the camera and releases every resource. */
  fun close(callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by SphereCameraHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
    /** Sets up an instance of `SphereCameraHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: SphereCameraHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.listCameras$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.listCameras{ result: Result<List<CameraDescriptor>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.open$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val cameraIdArg = args[0] as String
            val formatArg = args[1] as CaptureFormatRequest
            api.open(cameraIdArg, formatArg) { result: Result<CameraOpenResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.attachPreview$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.attachPreview{ result: Result<Long> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.detachPreview$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.detachPreview{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.meterAndLock$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val durationSecondsArg = args[0] as Double
            api.meterAndLock(durationSecondsArg) { result: Result<MeteringResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.unlock$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.unlock{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.captureBracket$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val evBiasesArg = args[0] as List<Double>
            val outputDirectoryArg = args[1] as String
            val namePrefixArg = args[2] as String
            api.captureBracket(evBiasesArg, outputDirectoryArg, namePrefixArg) { result: Result<CaptureResponse> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.thermalState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.thermalState{ result: Result<PlatformThermalState> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.sampleClockOffset$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.sampleClockOffset{ result: Result<ClockOffsetSample> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.deviceIdentity$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.deviceIdentity{ result: Result<DeviceIdentity> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.maxTextureSize$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.maxTextureSize{ result: Result<Long> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.totalPhysicalMemoryMb$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.totalPhysicalMemoryMb{ result: Result<Long> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.availableProcessMemoryMb$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.availableProcessMemoryMb{ result: Result<Long> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.batteryPercent$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.batteryPercent{ result: Result<Long> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.close$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.close{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/** Generated class from Pigeon that represents Flutter messages that can be called from Kotlin. */
class SphereCameraFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by SphereCameraFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
  }
  /**
   * A preview frame is ready, stamped on the shared monotonic base. Phase 07
   * pairs this with the pose stream.
   */
  fun onFrameAvailable(timestampUsArg: Long, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onFrameAvailable$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(timestampUsArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * Something went wrong outside a pending call — a disconnect, a HAL error,
   * a dropped burst.
   */
  fun onError(codeArg: String, messageArg: String, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onError$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(codeArg, messageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * Thermal state changed (§5). At `serious` the UI warns and suggests
   * stitching later; at `critical` a stitch is refused and told why.
   */
  fun onThermalStateChanged(stateArg: PlatformThermalState, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onThermalStateChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(stateArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * The session was interrupted or resumed — a phone call, another app, or
   * split view on iPad, where a second app taking the camera is common
   * (§7 pitfall 3).
   */
  fun onSessionInterrupted(interruptedArg: Boolean, reasonArg: String, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onSessionInterrupted$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(interruptedArg, reasonArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
/**
 * The AHRS half (Phase 07).
 *
 * Deliberately a second API rather than more methods on
 * [SphereCameraHostApi]: the pose stream outlives any one camera session — §6
 * pitfall 5 says never reset the buffer between targets — and it must keep
 * running while the camera is closed and reopened. Two APIs make that
 * independence structural instead of a convention someone has to remember.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface SpherePoseHostApi {
  /**
   * What this device's motion hardware can do. Cheap, and callable before
   * anything is started, because Phase 12 §1 refuses an unsupported device at
   * the *feature entry point* — before the camera opens, before the user has
   * walked to the first station.
   */
  fun poseCapabilities(callback: (Result<PoseCapabilities>) -> Unit)
  /**
   * Starts the attitude stream at [samplingPeriodUs] (§4 asks for 10 000 µs,
   * i.e. 100 Hz).
   *
   * Throws when the device is unsupported, so the refusal cannot be missed by
   * a caller that forgot to check [poseCapabilities] first.
   */
  fun startPose(samplingPeriodUs: Long, callback: (Result<PoseStreamInfo>) -> Unit)
  /** Stops the stream and releases the sensors. */
  fun stopPose(callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by SpherePoseHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
    /** Sets up an instance of `SpherePoseHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: SpherePoseHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.poseCapabilities$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.poseCapabilities{ result: Result<PoseCapabilities> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.startPose$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val samplingPeriodUsArg = args[0] as Long
            api.startPose(samplingPeriodUsArg) { result: Result<PoseStreamInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.stopPose$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopPose{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/** Generated class from Pigeon that represents Flutter messages that can be called from Kotlin. */
class SpherePoseFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by SpherePoseFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
  }
  /**
   * One attitude sample, at the requested rate.
   *
   * One message per sample rather than a batch: the guidance reticle of Phase
   * 09 is driven from the newest pose, and batching 100 Hz into 10 Hz packets
   * would add up to 100 ms of visible lag to the aim gate for no benefit the
   * interpolator needs — `PoseBuffer.at()` always looks *backwards* to a
   * shutter that has already happened, so delivery latency costs it nothing
   * and staleness costs the UI directly.
   */
  fun onPoseSample(sampleArg: PlatformPoseSample, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SpherePoseFlutterApi.onPoseSample$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(sampleArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * The stream failed after starting — the sensor was unregistered by the OS,
   * or motion updates stopped. Never silent (architecture §8).
   */
  fun onPoseError(codeArg: String, messageArg: String, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sphere_view.SpherePoseFlutterApi.onPoseError$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(codeArg, messageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MessagesPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
