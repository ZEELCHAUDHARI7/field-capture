// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class PigeonError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "PigeonError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? PigeonError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> PigeonError {
  return PigeonError(code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.", details: "")
}

enum MessagesPigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
  static func doubleEquals(_ lhs: Double, _ rhs: Double) -> Bool {
    return (lhs.isNaN && rhs.isNaN) || lhs == rhs
  }

  static func doubleHash(_ value: Double, _ hasher: inout Hasher) {
    if value.isNaN {
      hasher.combine(0x7FF8000000000000)
    } else {
      // Normalize -0.0 to 0.0
      hasher.combine(value == 0 ? 0 : value)
    }
  }

  static func deepEquals(_ lhs: Any?, _ rhs: Any?) -> Bool {
    let cleanLhs = nilOrValue(lhs) as Any?
    let cleanRhs = nilOrValue(rhs) as Any?
    switch (cleanLhs, cleanRhs) {
    case (nil, nil):
      return true

    case (nil, _), (_, nil):
      return false

    case (let lhs as AnyObject, let rhs as AnyObject) where lhs === rhs:
      return true

    case is (Void, Void):
      return true

    case (let lhsArray, let rhsArray) as ([Any?], [Any?]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !deepEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsArray, let rhsArray) as ([Double], [Double]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !doubleEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsDictionary, let rhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
      guard lhsDictionary.count == rhsDictionary.count else { return false }
      for (lhsKey, lhsValue) in lhsDictionary {
        var found = false
        for (rhsKey, rhsValue) in rhsDictionary {
          if deepEquals(lhsKey, rhsKey) {
            if deepEquals(lhsValue, rhsValue) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if !found { return false }
      }
      return true

    case (let lhs as Double, let rhs as Double):
      return doubleEquals(lhs, rhs)

    case (let lhsHashable, let rhsHashable) as (AnyHashable, AnyHashable):
      return lhsHashable == rhsHashable

    default:
      return false
    }
  }

  static func deepHash(value: Any?, hasher: inout Hasher) {
    let cleanValue = nilOrValue(value) as Any?
    if let cleanValue = cleanValue {
      if let doubleValue = cleanValue as? Double {
        doubleHash(doubleValue, &hasher)
      } else if let valueList = cleanValue as? [Any?] {
        for item in valueList {
          deepHash(value: item, hasher: &hasher)
        }
      } else if let valueList = cleanValue as? [Double] {
        for item in valueList {
          doubleHash(item, &hasher)
        }
      } else if let valueDict = cleanValue as? [AnyHashable: Any?] {
        var result = 0
        for (key, value) in valueDict {
          var entryKeyHasher = Hasher()
          deepHash(value: key, hasher: &entryKeyHasher)
          var entryValueHasher = Hasher()
          deepHash(value: value, hasher: &entryValueHasher)
          result = result &+ ((entryKeyHasher.finalize() &* 31) ^ entryValueHasher.finalize())
        }
        hasher.combine(result)
      } else if let hashableValue = cleanValue as? AnyHashable {
        hasher.combine(hashableValue)
      } else {
        hasher.combine(String(describing: cleanValue))
      }
    } else {
      hasher.combine(0)
    }
  }

}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}


/// Which way a physical camera points.
enum PlatformCameraFacing: Int, CaseIterable {
  case back = 0
  case front = 1
  case external = 2
}

/// The device thermal state, normalised across the two platforms'
/// differently-grained scales.
enum PlatformThermalState: Int, CaseIterable {
  case nominal = 0
  case fair = 1
  case serious = 2
  case critical = 3
}

/// How much of the AE/AWB/AF lock the platform actually granted.
enum PlatformLockQuality: Int, CaseIterable {
  case fullyLocked = 0
  case bestEffort = 1
  case unlocked = 2
}

/// Which clock a frame timestamp is expressed on *before* the plugin converts
/// it (§2.5, §3.5). Recorded rather than assumed, because the conversion is
/// different for each and getting it wrong is a silent 10–50 ms pose error.
enum PlatformTimestampBase: Int, CaseIterable {
  /// Android `SENSOR_INFO_TIMESTAMP_SOURCE == REALTIME`:
  /// `SystemClock.elapsedRealtimeNanos()`, the same base as
  /// `SensorEvent.timestamp`. Directly comparable, offset exactly zero.
  case androidRealtime = 0
  /// Android `SENSOR_INFO_TIMESTAMP_SOURCE == UNKNOWN`: `System.nanoTime()`,
  /// which is *not* the sensor base. An offset must be estimated.
  case androidMonotonicUnknown = 1
  /// iOS `CMClockGetHostTimeClock` (`mach_absolute_time`). `CMDeviceMotion`
  /// reports `systemUptime`; the plugin measures the offset rather than
  /// assuming the two are identical.
  case iosHostTime = 2
}

/// What the burst actually asks the sensor for.
enum PlatformCaptureFormat: Int, CaseIterable {
  /// Direct JPEG burst. The straightforward path.
  case jpeg = 0
  /// `YUV_420_888` burst with the JPEG encode deferred off the capture
  /// thread. R3 §9 found evidence (Nexus 5: ~243 ms JPEG stall vs ~0 ms YUV)
  /// that encoding, not sensor readout, dominates per-frame latency — so this
  /// is the cheapest way to recover a burst that misses the 600 ms budget,
  /// far cheaper than abandoning HDR. Android only.
  case yuvDeferredJpeg = 1
}

/// How the bracket was actually produced, so a degraded path is recorded
/// rather than mistaken for the real thing (architecture §8).
enum PlatformBracketMode: Int, CaseIterable {
  /// The intended path: explicit per-request exposure time with AE off.
  case manualExposureBurst = 0
  /// No `MANUAL_SENSOR`: `CONTROL_AE_EXPOSURE_COMPENSATION` per request. Not a
  /// true bracket — AE is still deciding — and it must not be read as one.
  case aeCompensationBurst = 1
  /// iOS `AVCapturePhotoBracketSettings` with manual exposure settings.
  case photoBracket = 2
  /// `maxBracketedCapturePhotoCount` came back below the requested count:
  /// sequential single shots with the exposure changed between them. Slower,
  /// and the frames are further apart in time.
  case sequentialManual = 3
  /// No usable exposure control at all. One frame at the metered lock.
  case singleShot = 4
}

/// Android `INFO_SUPPORTED_HARDWARE_LEVEL`. Recorded but **not** gated on —
/// R3 §8 is explicit that the gate is `MANUAL_SENSOR` in
/// `REQUEST_AVAILABLE_CAPABILITIES`, because `LIMITED` devices may or may not
/// have it. Both are reported so the findings can show whether the two ever
/// disagree on real fleet hardware.
enum PlatformHardwareLevel: Int, CaseIterable {
  case legacy = 0
  case limited = 1
  case full = 2
  case level3 = 3
  case external = 4
  case unknown = 5
}

/// Which reference frame a platform attitude quaternion is expressed against.
///
/// Carried on every sample rather than inferred from `Platform.isAndroid`,
/// because it is the input to the one conversion Phase 07 §2 explicitly says
/// **must not be trusted to derivation** — the doc's own Android derivation
/// produces a determinant −1 matrix, which would mirror the panorama. Naming
/// the source frame in the data is what lets `pose_frame_conversion.dart` hold
/// one tested conversion per frame instead of one per platform guess.
enum PlatformPoseFrame: Int, CaseIterable {
  /// Android `TYPE_GAME_ROTATION_VECTOR`: device→world, where the world frame
  /// is `X` ≈ east, `Y` ≈ north, **`Z` up**. Gyro + accelerometer, bias- and
  /// scale-corrected by the OS filter, and — the reason it is used instead of
  /// `TYPE_ROTATION_VECTOR` — **no magnetometer**, so rebar, lift motors and
  /// steel studs cannot bend the heading (Phase 07 §1, Math §1.1).
  case androidGameRotationVector = 0
  /// iOS `CMDeviceMotion.attitude.quaternion` under
  /// `.xArbitraryCorrectedZVertical`: device→reference, reference `Z`
  /// vertical, `X` an arbitrary horizontal direction, yaw drift-corrected.
  /// Chosen over `.xTrueNorthZVertical` for the same magnetometer reason.
  case iosXArbitraryCorrectedZVertical = 1
}

/// Which clock a pose sample's timestamp is on, before any conversion.
///
/// Separate from [PlatformTimestampBase], which describes the *camera*. Phase
/// 07 §6 pitfall 1 is that `SensorEvent.timestamp`'s base "varies by device" —
/// usually `elapsedRealtimeNanos`, but not universally — and Phase 06's
/// `TimestampMapper` already assumed the usual case when it put camera frames
/// on `elapsedRealtimeNanos` and called the offset exactly zero. That
/// assumption is now *measured* on the pose side rather than inherited, because
/// if it is wrong the two clocks silently disagree by however long the device
/// has been asleep.
enum PlatformPoseClockBase: Int, CaseIterable {
  /// `SensorEvent.timestamp` is `SystemClock.elapsedRealtimeNanos()`. The
  /// documented and near-universal case, and the one Phase 06's camera-side
  /// `REALTIME` mapping pairs with exactly.
  case androidElapsedRealtime = 0
  /// `SensorEvent.timestamp` is `System.nanoTime()` — the device that pitfall 1
  /// warns about. The offset is real, device-specific, and measured.
  case androidMonotonicNanoTime = 1
  /// `CMDeviceMotion.timestamp`, seconds since boot on `systemUptime` — the
  /// same base Phase 06's iOS `TimestampMapper` converts sample buffers onto.
  case iosSystemUptime = 2
}

/// Pixel dimensions. Not `dart:ui.Size` — see `ImageSize`, which this maps to.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PlatformSize: Hashable, CustomStringConvertible {
  var width: Int64
  var height: Int64


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PlatformSize? {
    let width = pigeonVar_list[0] as! Int64
    let height = pigeonVar_list[1] as! Int64

    return PlatformSize(
      width: width,
      height: height
    )
  }
  func toList() -> [Any?] {
    return [
      width,
      height,
    ]
  }
  static func == (lhs: PlatformSize, rhs: PlatformSize) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.width, rhs.width) && MessagesPigeonInternal.deepEquals(lhs.height, rhs.height)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PlatformSize")
    MessagesPigeonInternal.deepHash(value: width, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: height, hasher: &hasher)
  }

  public var description: String {
    return "PlatformSize(width: \(String(describing: width)), height: \(String(describing: height)))"
  }
}

/// A rectangle in sensor-array coordinates, `[left, top, width, height]`.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PlatformRect: Hashable, CustomStringConvertible {
  var left: Int64
  var top: Int64
  var width: Int64
  var height: Int64


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PlatformRect? {
    let left = pigeonVar_list[0] as! Int64
    let top = pigeonVar_list[1] as! Int64
    let width = pigeonVar_list[2] as! Int64
    let height = pigeonVar_list[3] as! Int64

    return PlatformRect(
      left: left,
      top: top,
      width: width,
      height: height
    )
  }
  func toList() -> [Any?] {
    return [
      left,
      top,
      width,
      height,
    ]
  }
  static func == (lhs: PlatformRect, rhs: PlatformRect) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.left, rhs.left) && MessagesPigeonInternal.deepEquals(lhs.top, rhs.top) && MessagesPigeonInternal.deepEquals(lhs.width, rhs.width) && MessagesPigeonInternal.deepEquals(lhs.height, rhs.height)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PlatformRect")
    MessagesPigeonInternal.deepHash(value: left, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: top, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: width, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: height, hasher: &hasher)
  }

  public var description: String {
    return "PlatformRect(left: \(String(describing: left)), top: \(String(describing: top)), width: \(String(describing: width)), height: \(String(describing: height)))"
  }
}

/// One physical camera, with enough capability detail for Dart to choose.
///
/// Generated class from Pigeon that represents data sent in messages.
struct CameraDescriptor: Hashable, CustomStringConvertible {
  var id: String
  var facing: PlatformCameraFacing
  /// Still-capture sizes offered, largest first.
  var availableSizes: [PlatformSize]
  /// `LENS_INFO_AVAILABLE_FOCAL_LENGTHS` on Android; the single lens focal on
  /// iOS where derivable. Used to exclude the ultra-wide: §2.1 picks the
  /// camera whose focal is *not* the shortest.
  var focalLengthsMm: [Double]
  /// Whether a real per-frame-controlled bracket is available.
  var supportsBracketing: Bool
  /// iOS `maxBracketedCapturePhotoCount`, queried at runtime after the format
  /// is set — R3 §4: it is not a fixed number and has no published per-device
  /// table. On Android this is the burst length the session will accept.
  var maxBracketCount: Int64
  /// Whether a lens distortion model can be obtained. Frequently false: R2
  /// found `LENS_DISTORTION` null even on Pixel hardware, and iOS only has a
  /// LUT on devices that deliver calibration data at all.
  var hasDistortionModel: Bool
  var hardwareLevel: PlatformHardwareLevel
  /// `MANUAL_SENSOR` in `REQUEST_AVAILABLE_CAPABILITIES`. **This** is the
  /// gate, not [hardwareLevel] (R3 §8).
  var hasManualSensor: Bool
  var isLogicalMultiCamera: Bool
  /// Why selection skipped this camera, when it did. §2.1 asks for the
  /// ultra-wide to be excluded *and logged*, since a future config may want it.
  var excludedReason: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> CameraDescriptor? {
    let id = pigeonVar_list[0] as! String
    let facing = pigeonVar_list[1] as! PlatformCameraFacing
    let availableSizes = pigeonVar_list[2] as! [PlatformSize]
    let focalLengthsMm = pigeonVar_list[3] as! [Double]
    let supportsBracketing = pigeonVar_list[4] as! Bool
    let maxBracketCount = pigeonVar_list[5] as! Int64
    let hasDistortionModel = pigeonVar_list[6] as! Bool
    let hardwareLevel = pigeonVar_list[7] as! PlatformHardwareLevel
    let hasManualSensor = pigeonVar_list[8] as! Bool
    let isLogicalMultiCamera = pigeonVar_list[9] as! Bool
    let excludedReason: String? = nilOrValue(pigeonVar_list[10])

    return CameraDescriptor(
      id: id,
      facing: facing,
      availableSizes: availableSizes,
      focalLengthsMm: focalLengthsMm,
      supportsBracketing: supportsBracketing,
      maxBracketCount: maxBracketCount,
      hasDistortionModel: hasDistortionModel,
      hardwareLevel: hardwareLevel,
      hasManualSensor: hasManualSensor,
      isLogicalMultiCamera: isLogicalMultiCamera,
      excludedReason: excludedReason
    )
  }
  func toList() -> [Any?] {
    return [
      id,
      facing,
      availableSizes,
      focalLengthsMm,
      supportsBracketing,
      maxBracketCount,
      hasDistortionModel,
      hardwareLevel,
      hasManualSensor,
      isLogicalMultiCamera,
      excludedReason,
    ]
  }
  static func == (lhs: CameraDescriptor, rhs: CameraDescriptor) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.id, rhs.id) && MessagesPigeonInternal.deepEquals(lhs.facing, rhs.facing) && MessagesPigeonInternal.deepEquals(lhs.availableSizes, rhs.availableSizes) && MessagesPigeonInternal.deepEquals(lhs.focalLengthsMm, rhs.focalLengthsMm) && MessagesPigeonInternal.deepEquals(lhs.supportsBracketing, rhs.supportsBracketing) && MessagesPigeonInternal.deepEquals(lhs.maxBracketCount, rhs.maxBracketCount) && MessagesPigeonInternal.deepEquals(lhs.hasDistortionModel, rhs.hasDistortionModel) && MessagesPigeonInternal.deepEquals(lhs.hardwareLevel, rhs.hardwareLevel) && MessagesPigeonInternal.deepEquals(lhs.hasManualSensor, rhs.hasManualSensor) && MessagesPigeonInternal.deepEquals(lhs.isLogicalMultiCamera, rhs.isLogicalMultiCamera) && MessagesPigeonInternal.deepEquals(lhs.excludedReason, rhs.excludedReason)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("CameraDescriptor")
    MessagesPigeonInternal.deepHash(value: id, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: facing, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: availableSizes, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: focalLengthsMm, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: supportsBracketing, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: maxBracketCount, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hasDistortionModel, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hardwareLevel, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hasManualSensor, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: isLogicalMultiCamera, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: excludedReason, hasher: &hasher)
  }

  public var description: String {
    return "CameraDescriptor(id: \(String(describing: id)), facing: \(String(describing: facing)), availableSizes: \(String(describing: availableSizes)), focalLengthsMm: \(String(describing: focalLengthsMm)), supportsBracketing: \(String(describing: supportsBracketing)), maxBracketCount: \(String(describing: maxBracketCount)), hasDistortionModel: \(String(describing: hasDistortionModel)), hardwareLevel: \(String(describing: hardwareLevel)), hasManualSensor: \(String(describing: hasManualSensor)), isLogicalMultiCamera: \(String(describing: isLogicalMultiCamera)), excludedReason: \(String(describing: excludedReason)))"
  }
}

/// What Dart asks the platform to configure.
///
/// Generated class from Pigeon that represents data sent in messages.
struct CaptureFormatRequest: Hashable, CustomStringConvertible {
  /// `null` means "pick the largest, subject to [preferFourThree]".
  var captureSize: PlatformSize? = nil
  /// Prefer a 4:3 output: it matches the sensor's full active array, so no
  /// crop factor enters the intrinsics (§2.1, Math §4.1), and it gives the
  /// largest vertical FOV, which directly reduces the number of rings.
  var preferFourThree: Bool
  /// ~1280. Preview is only used for aiming; a full-resolution preview on a
  /// tablet burns battery and thermal headroom the stitch will need (§4).
  var previewTargetWidth: Int64
  var format: PlatformCaptureFormat
  var jpegQuality: Int64
  /// Compute a centre-crop mean RGB per delivered frame. Off in production;
  /// on for the grey-card lock test (§6), which needs a *measurement* rather
  /// than an eyeball.
  var computeFrameStatistics: Bool


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> CaptureFormatRequest? {
    let captureSize: PlatformSize? = nilOrValue(pigeonVar_list[0])
    let preferFourThree = pigeonVar_list[1] as! Bool
    let previewTargetWidth = pigeonVar_list[2] as! Int64
    let format = pigeonVar_list[3] as! PlatformCaptureFormat
    let jpegQuality = pigeonVar_list[4] as! Int64
    let computeFrameStatistics = pigeonVar_list[5] as! Bool

    return CaptureFormatRequest(
      captureSize: captureSize,
      preferFourThree: preferFourThree,
      previewTargetWidth: previewTargetWidth,
      format: format,
      jpegQuality: jpegQuality,
      computeFrameStatistics: computeFrameStatistics
    )
  }
  func toList() -> [Any?] {
    return [
      captureSize,
      preferFourThree,
      previewTargetWidth,
      format,
      jpegQuality,
      computeFrameStatistics,
    ]
  }
  static func == (lhs: CaptureFormatRequest, rhs: CaptureFormatRequest) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.captureSize, rhs.captureSize) && MessagesPigeonInternal.deepEquals(lhs.preferFourThree, rhs.preferFourThree) && MessagesPigeonInternal.deepEquals(lhs.previewTargetWidth, rhs.previewTargetWidth) && MessagesPigeonInternal.deepEquals(lhs.format, rhs.format) && MessagesPigeonInternal.deepEquals(lhs.jpegQuality, rhs.jpegQuality) && MessagesPigeonInternal.deepEquals(lhs.computeFrameStatistics, rhs.computeFrameStatistics)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("CaptureFormatRequest")
    MessagesPigeonInternal.deepHash(value: captureSize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: preferFourThree, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: previewTargetWidth, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: format, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: jpegQuality, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: computeFrameStatistics, hasher: &hasher)
  }

  public var description: String {
    return "CaptureFormatRequest(captureSize: \(String(describing: captureSize)), preferFourThree: \(String(describing: preferFourThree)), previewTargetWidth: \(String(describing: previewTargetWidth)), format: \(String(describing: format)), jpegQuality: \(String(describing: jpegQuality)), computeFrameStatistics: \(String(describing: computeFrameStatistics)))"
  }
}

/// The raw Android facts Math §4.1 derives intrinsics from. Every field is
/// straight out of `CameraCharacteristics` or the configured stream — no
/// arithmetic happens on the Kotlin side.
///
/// Generated class from Pigeon that represents data sent in messages.
struct AndroidIntrinsicFacts: Hashable, CustomStringConvertible {
  /// `LENS_INFO_AVAILABLE_FOCAL_LENGTHS[i]` actually in force.
  var focalLengthMm: Double? = nil
  /// `SENSOR_INFO_PHYSICAL_SIZE`.
  var sensorPhysicalWidthMm: Double? = nil
  var sensorPhysicalHeightMm: Double? = nil
  /// `SENSOR_INFO_PIXEL_ARRAY_SIZE` — the denominator of the pixel pitch.
  var pixelArraySize: PlatformSize? = nil
  /// `SENSOR_INFO_PRE_CORRECTION_ACTIVE_ARRAY_SIZE`. Math §4.1 anchors
  /// *every* coordinate to this frame.
  var preCorrectionActiveArray: PlatformRect? = nil
  /// `SENSOR_INFO_ACTIVE_ARRAY_SIZE`, for the [activeArraysDiffer] check.
  var activeArray: PlatformRect? = nil
  /// The `SCALER_CROP_REGION` actually applied. Full array when not zoomed.
  var cropRegion: PlatformRect? = nil
  /// `LENS_INTRINSIC_CALIBRATION` = `[fx, fy, cx, cy, s]` in
  /// pre-correction-array pixels, or null. R2: gated by no capability flag,
  /// documented "may be null", reported null even on Pixel hardware — so this
  /// is an **override**, not the primary path.
  var lensIntrinsicCalibration: [Double]? = nil
  /// `LENS_DISTORTION` = `[κ1, κ2, κ3, κ4, κ5]`, **unreordered**. The reorder
  /// to OpenCV's `(k1, k2, p1, p2, k3)` happens in Dart, in the one place it
  /// already lives.
  var lensDistortion: [Double]? = nil
  /// Whether `DISTORTION_CORRECTION_MODE_OFF` was requested on every capture.
  var distortionCorrectionModeOffRequested: Bool
  /// Whether this device lists any mode other than `OFF`. R2 §4: most do not,
  /// and where none does, the pre-correction/active distinction is moot.
  var distortionCorrectionSupportsNonOff: Bool
  /// Whether the two array rectangles actually differ on this device.
  var activeArraysDiffer: Bool
  /// EXIF last resort: `fx = W · FocalLengthIn35mmFilm / 36`.
  var focalLengthIn35mmFilm: Double? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> AndroidIntrinsicFacts? {
    let focalLengthMm: Double? = nilOrValue(pigeonVar_list[0])
    let sensorPhysicalWidthMm: Double? = nilOrValue(pigeonVar_list[1])
    let sensorPhysicalHeightMm: Double? = nilOrValue(pigeonVar_list[2])
    let pixelArraySize: PlatformSize? = nilOrValue(pigeonVar_list[3])
    let preCorrectionActiveArray: PlatformRect? = nilOrValue(pigeonVar_list[4])
    let activeArray: PlatformRect? = nilOrValue(pigeonVar_list[5])
    let cropRegion: PlatformRect? = nilOrValue(pigeonVar_list[6])
    let lensIntrinsicCalibration: [Double]? = nilOrValue(pigeonVar_list[7])
    let lensDistortion: [Double]? = nilOrValue(pigeonVar_list[8])
    let distortionCorrectionModeOffRequested = pigeonVar_list[9] as! Bool
    let distortionCorrectionSupportsNonOff = pigeonVar_list[10] as! Bool
    let activeArraysDiffer = pigeonVar_list[11] as! Bool
    let focalLengthIn35mmFilm: Double? = nilOrValue(pigeonVar_list[12])

    return AndroidIntrinsicFacts(
      focalLengthMm: focalLengthMm,
      sensorPhysicalWidthMm: sensorPhysicalWidthMm,
      sensorPhysicalHeightMm: sensorPhysicalHeightMm,
      pixelArraySize: pixelArraySize,
      preCorrectionActiveArray: preCorrectionActiveArray,
      activeArray: activeArray,
      cropRegion: cropRegion,
      lensIntrinsicCalibration: lensIntrinsicCalibration,
      lensDistortion: lensDistortion,
      distortionCorrectionModeOffRequested: distortionCorrectionModeOffRequested,
      distortionCorrectionSupportsNonOff: distortionCorrectionSupportsNonOff,
      activeArraysDiffer: activeArraysDiffer,
      focalLengthIn35mmFilm: focalLengthIn35mmFilm
    )
  }
  func toList() -> [Any?] {
    return [
      focalLengthMm,
      sensorPhysicalWidthMm,
      sensorPhysicalHeightMm,
      pixelArraySize,
      preCorrectionActiveArray,
      activeArray,
      cropRegion,
      lensIntrinsicCalibration,
      lensDistortion,
      distortionCorrectionModeOffRequested,
      distortionCorrectionSupportsNonOff,
      activeArraysDiffer,
      focalLengthIn35mmFilm,
    ]
  }
  static func == (lhs: AndroidIntrinsicFacts, rhs: AndroidIntrinsicFacts) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.focalLengthMm, rhs.focalLengthMm) && MessagesPigeonInternal.deepEquals(lhs.sensorPhysicalWidthMm, rhs.sensorPhysicalWidthMm) && MessagesPigeonInternal.deepEquals(lhs.sensorPhysicalHeightMm, rhs.sensorPhysicalHeightMm) && MessagesPigeonInternal.deepEquals(lhs.pixelArraySize, rhs.pixelArraySize) && MessagesPigeonInternal.deepEquals(lhs.preCorrectionActiveArray, rhs.preCorrectionActiveArray) && MessagesPigeonInternal.deepEquals(lhs.activeArray, rhs.activeArray) && MessagesPigeonInternal.deepEquals(lhs.cropRegion, rhs.cropRegion) && MessagesPigeonInternal.deepEquals(lhs.lensIntrinsicCalibration, rhs.lensIntrinsicCalibration) && MessagesPigeonInternal.deepEquals(lhs.lensDistortion, rhs.lensDistortion) && MessagesPigeonInternal.deepEquals(lhs.distortionCorrectionModeOffRequested, rhs.distortionCorrectionModeOffRequested) && MessagesPigeonInternal.deepEquals(lhs.distortionCorrectionSupportsNonOff, rhs.distortionCorrectionSupportsNonOff) && MessagesPigeonInternal.deepEquals(lhs.activeArraysDiffer, rhs.activeArraysDiffer) && MessagesPigeonInternal.deepEquals(lhs.focalLengthIn35mmFilm, rhs.focalLengthIn35mmFilm)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("AndroidIntrinsicFacts")
    MessagesPigeonInternal.deepHash(value: focalLengthMm, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: sensorPhysicalWidthMm, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: sensorPhysicalHeightMm, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: pixelArraySize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: preCorrectionActiveArray, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: activeArray, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: cropRegion, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lensIntrinsicCalibration, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lensDistortion, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: distortionCorrectionModeOffRequested, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: distortionCorrectionSupportsNonOff, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: activeArraysDiffer, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: focalLengthIn35mmFilm, hasher: &hasher)
  }

  public var description: String {
    return "AndroidIntrinsicFacts(focalLengthMm: \(String(describing: focalLengthMm)), sensorPhysicalWidthMm: \(String(describing: sensorPhysicalWidthMm)), sensorPhysicalHeightMm: \(String(describing: sensorPhysicalHeightMm)), pixelArraySize: \(String(describing: pixelArraySize)), preCorrectionActiveArray: \(String(describing: preCorrectionActiveArray)), activeArray: \(String(describing: activeArray)), cropRegion: \(String(describing: cropRegion)), lensIntrinsicCalibration: \(String(describing: lensIntrinsicCalibration)), lensDistortion: \(String(describing: lensDistortion)), distortionCorrectionModeOffRequested: \(String(describing: distortionCorrectionModeOffRequested)), distortionCorrectionSupportsNonOff: \(String(describing: distortionCorrectionSupportsNonOff)), activeArraysDiffer: \(String(describing: activeArraysDiffer)), focalLengthIn35mmFilm: \(String(describing: focalLengthIn35mmFilm)))"
  }
}

/// The raw iOS facts Math §4.2 derives intrinsics from, one field per tier of
/// the chain so the resolver can say which rung it reached.
///
/// Generated class from Pigeon that represents data sent in messages.
struct IosIntrinsicFacts: Hashable, CustomStringConvertible {
  /// Tier 1: `AVCameraCalibrationData.intrinsicMatrix`, row-major 3×3, valid
  /// for [calibrationReferenceSize]. R2: needs a multi-camera virtual device,
  /// which excludes base iPad, Air and mini outright — expect null.
  var calibrationIntrinsicMatrix: [Double]? = nil
  var calibrationReferenceSize: PlatformSize? = nil
  /// Tier 2: `AVCaptureConnection.cameraIntrinsicMatrix`, delivered as a
  /// `CMSampleBuffer` attachment on `AVCaptureVideoDataOutput` — *not* photo
  /// output. The only measured path on a single-lens iPad, and its
  /// availability there is contested (R2 "Still unknown" #1).
  var connectionIntrinsicMatrix: [Double]? = nil
  var connectionIntrinsicReferenceSize: PlatformSize? = nil
  /// The capability flag says "supported"…
  var connectionIntrinsicDeliverySupported: Bool
  /// …and this says the attachment *actually arrived on real frames*. The
  /// distinction is the whole point: reporting the flag alone is the mistake
  /// that left this question open since 2017 (Spike B).
  var connectionIntrinsicAttachmentArrived: Bool
  /// Tier 3, and the realistic primary: `videoFieldOfView`, confirmed by R2 to
  /// be **horizontal**. `fx = (W/2) / tan(FOV/2)`. This is *not*
  /// `geometricDistortionCorrectedVideoFieldOfView`, which describes the
  /// post-GDC frame and only applies while GDC is on.
  var videoFieldOfViewDegrees: Double
  /// `lensDistortionLookupTable` — radial magnification factors from
  /// [lensDistortionCenterX]/[lensDistortionCenterY] to the farthest corner.
  /// Purely radial: the Brown–Conrady fit forces `p1 = p2 = 0`.
  var lensDistortionLookupTable: [Double]? = nil
  var lensDistortionCenterX: Double? = nil
  var lensDistortionCenterY: Double? = nil
  /// Mandatory on every capture, whichever tier is used (R2 §8). Reported so
  /// a violation is visible rather than assumed away.
  var geometricDistortionCorrectionDisabled: Bool
  var contentAwareDistortionCorrectionDisabled: Bool
  var calibrationDataDeliverySupported: Bool
  var isVirtualDevice: Bool
  /// EXIF last resort.
  var focalLengthIn35mmFilm: Double? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> IosIntrinsicFacts? {
    let calibrationIntrinsicMatrix: [Double]? = nilOrValue(pigeonVar_list[0])
    let calibrationReferenceSize: PlatformSize? = nilOrValue(pigeonVar_list[1])
    let connectionIntrinsicMatrix: [Double]? = nilOrValue(pigeonVar_list[2])
    let connectionIntrinsicReferenceSize: PlatformSize? = nilOrValue(pigeonVar_list[3])
    let connectionIntrinsicDeliverySupported = pigeonVar_list[4] as! Bool
    let connectionIntrinsicAttachmentArrived = pigeonVar_list[5] as! Bool
    let videoFieldOfViewDegrees = pigeonVar_list[6] as! Double
    let lensDistortionLookupTable: [Double]? = nilOrValue(pigeonVar_list[7])
    let lensDistortionCenterX: Double? = nilOrValue(pigeonVar_list[8])
    let lensDistortionCenterY: Double? = nilOrValue(pigeonVar_list[9])
    let geometricDistortionCorrectionDisabled = pigeonVar_list[10] as! Bool
    let contentAwareDistortionCorrectionDisabled = pigeonVar_list[11] as! Bool
    let calibrationDataDeliverySupported = pigeonVar_list[12] as! Bool
    let isVirtualDevice = pigeonVar_list[13] as! Bool
    let focalLengthIn35mmFilm: Double? = nilOrValue(pigeonVar_list[14])

    return IosIntrinsicFacts(
      calibrationIntrinsicMatrix: calibrationIntrinsicMatrix,
      calibrationReferenceSize: calibrationReferenceSize,
      connectionIntrinsicMatrix: connectionIntrinsicMatrix,
      connectionIntrinsicReferenceSize: connectionIntrinsicReferenceSize,
      connectionIntrinsicDeliverySupported: connectionIntrinsicDeliverySupported,
      connectionIntrinsicAttachmentArrived: connectionIntrinsicAttachmentArrived,
      videoFieldOfViewDegrees: videoFieldOfViewDegrees,
      lensDistortionLookupTable: lensDistortionLookupTable,
      lensDistortionCenterX: lensDistortionCenterX,
      lensDistortionCenterY: lensDistortionCenterY,
      geometricDistortionCorrectionDisabled: geometricDistortionCorrectionDisabled,
      contentAwareDistortionCorrectionDisabled: contentAwareDistortionCorrectionDisabled,
      calibrationDataDeliverySupported: calibrationDataDeliverySupported,
      isVirtualDevice: isVirtualDevice,
      focalLengthIn35mmFilm: focalLengthIn35mmFilm
    )
  }
  func toList() -> [Any?] {
    return [
      calibrationIntrinsicMatrix,
      calibrationReferenceSize,
      connectionIntrinsicMatrix,
      connectionIntrinsicReferenceSize,
      connectionIntrinsicDeliverySupported,
      connectionIntrinsicAttachmentArrived,
      videoFieldOfViewDegrees,
      lensDistortionLookupTable,
      lensDistortionCenterX,
      lensDistortionCenterY,
      geometricDistortionCorrectionDisabled,
      contentAwareDistortionCorrectionDisabled,
      calibrationDataDeliverySupported,
      isVirtualDevice,
      focalLengthIn35mmFilm,
    ]
  }
  static func == (lhs: IosIntrinsicFacts, rhs: IosIntrinsicFacts) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.calibrationIntrinsicMatrix, rhs.calibrationIntrinsicMatrix) && MessagesPigeonInternal.deepEquals(lhs.calibrationReferenceSize, rhs.calibrationReferenceSize) && MessagesPigeonInternal.deepEquals(lhs.connectionIntrinsicMatrix, rhs.connectionIntrinsicMatrix) && MessagesPigeonInternal.deepEquals(lhs.connectionIntrinsicReferenceSize, rhs.connectionIntrinsicReferenceSize) && MessagesPigeonInternal.deepEquals(lhs.connectionIntrinsicDeliverySupported, rhs.connectionIntrinsicDeliverySupported) && MessagesPigeonInternal.deepEquals(lhs.connectionIntrinsicAttachmentArrived, rhs.connectionIntrinsicAttachmentArrived) && MessagesPigeonInternal.deepEquals(lhs.videoFieldOfViewDegrees, rhs.videoFieldOfViewDegrees) && MessagesPigeonInternal.deepEquals(lhs.lensDistortionLookupTable, rhs.lensDistortionLookupTable) && MessagesPigeonInternal.deepEquals(lhs.lensDistortionCenterX, rhs.lensDistortionCenterX) && MessagesPigeonInternal.deepEquals(lhs.lensDistortionCenterY, rhs.lensDistortionCenterY) && MessagesPigeonInternal.deepEquals(lhs.geometricDistortionCorrectionDisabled, rhs.geometricDistortionCorrectionDisabled) && MessagesPigeonInternal.deepEquals(lhs.contentAwareDistortionCorrectionDisabled, rhs.contentAwareDistortionCorrectionDisabled) && MessagesPigeonInternal.deepEquals(lhs.calibrationDataDeliverySupported, rhs.calibrationDataDeliverySupported) && MessagesPigeonInternal.deepEquals(lhs.isVirtualDevice, rhs.isVirtualDevice) && MessagesPigeonInternal.deepEquals(lhs.focalLengthIn35mmFilm, rhs.focalLengthIn35mmFilm)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("IosIntrinsicFacts")
    MessagesPigeonInternal.deepHash(value: calibrationIntrinsicMatrix, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: calibrationReferenceSize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: connectionIntrinsicMatrix, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: connectionIntrinsicReferenceSize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: connectionIntrinsicDeliverySupported, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: connectionIntrinsicAttachmentArrived, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: videoFieldOfViewDegrees, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lensDistortionLookupTable, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lensDistortionCenterX, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lensDistortionCenterY, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: geometricDistortionCorrectionDisabled, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: contentAwareDistortionCorrectionDisabled, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: calibrationDataDeliverySupported, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: isVirtualDevice, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: focalLengthIn35mmFilm, hasher: &hasher)
  }

  public var description: String {
    return "IosIntrinsicFacts(calibrationIntrinsicMatrix: \(String(describing: calibrationIntrinsicMatrix)), calibrationReferenceSize: \(String(describing: calibrationReferenceSize)), connectionIntrinsicMatrix: \(String(describing: connectionIntrinsicMatrix)), connectionIntrinsicReferenceSize: \(String(describing: connectionIntrinsicReferenceSize)), connectionIntrinsicDeliverySupported: \(String(describing: connectionIntrinsicDeliverySupported)), connectionIntrinsicAttachmentArrived: \(String(describing: connectionIntrinsicAttachmentArrived)), videoFieldOfViewDegrees: \(String(describing: videoFieldOfViewDegrees)), lensDistortionLookupTable: \(String(describing: lensDistortionLookupTable)), lensDistortionCenterX: \(String(describing: lensDistortionCenterX)), lensDistortionCenterY: \(String(describing: lensDistortionCenterY)), geometricDistortionCorrectionDisabled: \(String(describing: geometricDistortionCorrectionDisabled)), contentAwareDistortionCorrectionDisabled: \(String(describing: contentAwareDistortionCorrectionDisabled)), calibrationDataDeliverySupported: \(String(describing: calibrationDataDeliverySupported)), isVirtualDevice: \(String(describing: isVirtualDevice)), focalLengthIn35mmFilm: \(String(describing: focalLengthIn35mmFilm)))"
  }
}

/// How frame timestamps were converted to the one monotonic microsecond base
/// the pose stream also uses (§2.5, §3.5).
///
/// This travels into the bundle. A 10–50 ms mismatch is 0.6–3° of rotation
/// error at a realistic 60°/s pan — larger than everything Phase 03 works to
/// remove, and it presents as a stitcher bug. If it ever goes wrong, this
/// record is how it gets found.
///
/// Generated class from Pigeon that represents data sent in messages.
struct ClockSyncInfo: Hashable, CustomStringConvertible {
  var base: PlatformTimestampBase
  /// Added to the camera's raw timestamp to land on the sensor/motion clock.
  /// Exactly `0` for [PlatformTimestampBase.androidRealtime].
  var offsetUs: Int64
  /// Half the spread of the offset estimator's samples. Zero when the two
  /// clocks are the same clock and no estimation was needed.
  var uncertaintyUs: Int64
  var note: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> ClockSyncInfo? {
    let base = pigeonVar_list[0] as! PlatformTimestampBase
    let offsetUs = pigeonVar_list[1] as! Int64
    let uncertaintyUs = pigeonVar_list[2] as! Int64
    let note = pigeonVar_list[3] as! String

    return ClockSyncInfo(
      base: base,
      offsetUs: offsetUs,
      uncertaintyUs: uncertaintyUs,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      base,
      offsetUs,
      uncertaintyUs,
      note,
    ]
  }
  static func == (lhs: ClockSyncInfo, rhs: ClockSyncInfo) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.base, rhs.base) && MessagesPigeonInternal.deepEquals(lhs.offsetUs, rhs.offsetUs) && MessagesPigeonInternal.deepEquals(lhs.uncertaintyUs, rhs.uncertaintyUs) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("ClockSyncInfo")
    MessagesPigeonInternal.deepHash(value: base, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: offsetUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: uncertaintyUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "ClockSyncInfo(base: \(String(describing: base)), offsetUs: \(String(describing: offsetUs)), uncertaintyUs: \(String(describing: uncertaintyUs)), note: \(String(describing: note)))"
  }
}

/// One instantaneous re-measurement of the two clocks, for the §6 stability
/// test ("offset stable within ±2 ms over 60 s").
///
/// Generated class from Pigeon that represents data sent in messages.
struct ClockOffsetSample: Hashable, CustomStringConvertible {
  var cameraClockUs: Int64
  var motionClockUs: Int64
  var offsetUs: Int64
  var uncertaintyUs: Int64


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> ClockOffsetSample? {
    let cameraClockUs = pigeonVar_list[0] as! Int64
    let motionClockUs = pigeonVar_list[1] as! Int64
    let offsetUs = pigeonVar_list[2] as! Int64
    let uncertaintyUs = pigeonVar_list[3] as! Int64

    return ClockOffsetSample(
      cameraClockUs: cameraClockUs,
      motionClockUs: motionClockUs,
      offsetUs: offsetUs,
      uncertaintyUs: uncertaintyUs
    )
  }
  func toList() -> [Any?] {
    return [
      cameraClockUs,
      motionClockUs,
      offsetUs,
      uncertaintyUs,
    ]
  }
  static func == (lhs: ClockOffsetSample, rhs: ClockOffsetSample) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.cameraClockUs, rhs.cameraClockUs) && MessagesPigeonInternal.deepEquals(lhs.motionClockUs, rhs.motionClockUs) && MessagesPigeonInternal.deepEquals(lhs.offsetUs, rhs.offsetUs) && MessagesPigeonInternal.deepEquals(lhs.uncertaintyUs, rhs.uncertaintyUs)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("ClockOffsetSample")
    MessagesPigeonInternal.deepHash(value: cameraClockUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: motionClockUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: offsetUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: uncertaintyUs, hasher: &hasher)
  }

  public var description: String {
    return "ClockOffsetSample(cameraClockUs: \(String(describing: cameraClockUs)), motionClockUs: \(String(describing: motionClockUs)), offsetUs: \(String(describing: offsetUs)), uncertaintyUs: \(String(describing: uncertaintyUs)))"
  }
}

/// The result of opening a camera: everything later stages are only valid for.
///
/// Generated class from Pigeon that represents data sent in messages.
struct CameraOpenResult: Hashable, CustomStringConvertible {
  var captureSize: PlatformSize
  var previewSize: PlatformSize
  /// Sensor mounting rotation, needed to reconcile the portrait lock with the
  /// sensor's native orientation.
  var sensorOrientationDegrees: Int64
  /// Clockwise degrees the **preview texture still needs** to stand upright on a
  /// portrait screen: `0`, `90`, `180` or `270`.
  ///
  /// The platform states this rather than Dart inferring it, and the difference is
  /// not academic — it is the bug that took four attempts to find. Every input to
  /// the inference is something the device says about itself: the sensor mounting,
  /// the reported preview size, and — the one that actually decided it — whether
  /// the render path already applied the rotation. On Android that last answer
  /// comes from `SurfaceProducer.handlesCropAndRotation()`, is `true` on the legacy
  /// texture path and `false` on the API 29+ `ImageReader` backend, and cannot be
  /// derived from anything visible to Dart. A derivation that got the first two
  /// right still turned a correct preview sideways.
  ///
  /// So: `0` when the buffer arrives upright, and the mounting angle when it does
  /// not. Dart applies exactly this and nothing else.
  var previewRotationDegrees: Int64
  /// Whether the render path handled the crop and rotation metadata itself.
  ///
  /// Reported alongside [previewRotationDegrees] rather than folded into it because
  /// the two answer different questions — "how far do I turn it" and "why" — and the
  /// second is what makes a bug report from an unfamiliar device actionable. It
  /// lands in `bundle.json`, so a capture carries the reason with it.
  var previewHandlesRotation: Bool
  var clock: ClockSyncInfo
  /// The bracket path this device will actually take, decided at open time so
  /// the caller can adjust the plan rather than discover it at frame 1.
  var bracketMode: PlatformBracketMode
  var maxBracketCount: Int64
  /// False means a crop factor entered the intrinsics and §2.1's preference
  /// could not be honoured. Worth surfacing: it changes the derivation.
  var captureAspectIsFourThree: Bool
  var androidFacts: AndroidIntrinsicFacts? = nil
  var iosFacts: IosIntrinsicFacts? = nil
  /// Any compromise made while opening. Never silently degrade (arch §8).
  var warning: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> CameraOpenResult? {
    let captureSize = pigeonVar_list[0] as! PlatformSize
    let previewSize = pigeonVar_list[1] as! PlatformSize
    let sensorOrientationDegrees = pigeonVar_list[2] as! Int64
    let previewRotationDegrees = pigeonVar_list[3] as! Int64
    let previewHandlesRotation = pigeonVar_list[4] as! Bool
    let clock = pigeonVar_list[5] as! ClockSyncInfo
    let bracketMode = pigeonVar_list[6] as! PlatformBracketMode
    let maxBracketCount = pigeonVar_list[7] as! Int64
    let captureAspectIsFourThree = pigeonVar_list[8] as! Bool
    let androidFacts: AndroidIntrinsicFacts? = nilOrValue(pigeonVar_list[9])
    let iosFacts: IosIntrinsicFacts? = nilOrValue(pigeonVar_list[10])
    let warning: String? = nilOrValue(pigeonVar_list[11])

    return CameraOpenResult(
      captureSize: captureSize,
      previewSize: previewSize,
      sensorOrientationDegrees: sensorOrientationDegrees,
      previewRotationDegrees: previewRotationDegrees,
      previewHandlesRotation: previewHandlesRotation,
      clock: clock,
      bracketMode: bracketMode,
      maxBracketCount: maxBracketCount,
      captureAspectIsFourThree: captureAspectIsFourThree,
      androidFacts: androidFacts,
      iosFacts: iosFacts,
      warning: warning
    )
  }
  func toList() -> [Any?] {
    return [
      captureSize,
      previewSize,
      sensorOrientationDegrees,
      previewRotationDegrees,
      previewHandlesRotation,
      clock,
      bracketMode,
      maxBracketCount,
      captureAspectIsFourThree,
      androidFacts,
      iosFacts,
      warning,
    ]
  }
  static func == (lhs: CameraOpenResult, rhs: CameraOpenResult) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.captureSize, rhs.captureSize) && MessagesPigeonInternal.deepEquals(lhs.previewSize, rhs.previewSize) && MessagesPigeonInternal.deepEquals(lhs.sensorOrientationDegrees, rhs.sensorOrientationDegrees) && MessagesPigeonInternal.deepEquals(lhs.previewRotationDegrees, rhs.previewRotationDegrees) && MessagesPigeonInternal.deepEquals(lhs.previewHandlesRotation, rhs.previewHandlesRotation) && MessagesPigeonInternal.deepEquals(lhs.clock, rhs.clock) && MessagesPigeonInternal.deepEquals(lhs.bracketMode, rhs.bracketMode) && MessagesPigeonInternal.deepEquals(lhs.maxBracketCount, rhs.maxBracketCount) && MessagesPigeonInternal.deepEquals(lhs.captureAspectIsFourThree, rhs.captureAspectIsFourThree) && MessagesPigeonInternal.deepEquals(lhs.androidFacts, rhs.androidFacts) && MessagesPigeonInternal.deepEquals(lhs.iosFacts, rhs.iosFacts) && MessagesPigeonInternal.deepEquals(lhs.warning, rhs.warning)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("CameraOpenResult")
    MessagesPigeonInternal.deepHash(value: captureSize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: previewSize, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: sensorOrientationDegrees, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: previewRotationDegrees, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: previewHandlesRotation, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: clock, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: bracketMode, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: maxBracketCount, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: captureAspectIsFourThree, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: androidFacts, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: iosFacts, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: warning, hasher: &hasher)
  }

  public var description: String {
    return "CameraOpenResult(captureSize: \(String(describing: captureSize)), previewSize: \(String(describing: previewSize)), sensorOrientationDegrees: \(String(describing: sensorOrientationDegrees)), previewRotationDegrees: \(String(describing: previewRotationDegrees)), previewHandlesRotation: \(String(describing: previewHandlesRotation)), clock: \(String(describing: clock)), bracketMode: \(String(describing: bracketMode)), maxBracketCount: \(String(describing: maxBracketCount)), captureAspectIsFourThree: \(String(describing: captureAspectIsFourThree)), androidFacts: \(String(describing: androidFacts)), iosFacts: \(String(describing: iosFacts)), warning: \(String(describing: warning)))"
  }
}

/// Who made this device and what it is called.
///
/// Phase 11 §2 writes these into the output's EXIF `Make`/`Model`. That is not
/// bookkeeping: a construction record is looked at months later by somebody
/// deciding whether to trust it, and "which tablet took this" is the first
/// question when one station's panoramas are visibly worse than the rest of the
/// walk. It is also the only way a field failure can be tied to a device model
/// without asking the person who captured it to remember.
///
/// Generated class from Pigeon that represents data sent in messages.
struct DeviceIdentity: Hashable, CustomStringConvertible {
  /// `Build.MANUFACTURER` on Android; `Apple` on iOS.
  var make: String
  /// `Build.MODEL` on Android; the hardware identifier (`iPad14,3`) on iOS.
  ///
  /// The identifier rather than the marketing name, because the marketing name
  /// needs a lookup table that goes stale with every release, and the
  /// identifier is what a capability question is actually answered against.
  var model: String
  /// OS release string, for the bundle's diagnostic record.
  var osVersion: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> DeviceIdentity? {
    let make = pigeonVar_list[0] as! String
    let model = pigeonVar_list[1] as! String
    let osVersion = pigeonVar_list[2] as! String

    return DeviceIdentity(
      make: make,
      model: model,
      osVersion: osVersion
    )
  }
  func toList() -> [Any?] {
    return [
      make,
      model,
      osVersion,
    ]
  }
  static func == (lhs: DeviceIdentity, rhs: DeviceIdentity) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.make, rhs.make) && MessagesPigeonInternal.deepEquals(lhs.model, rhs.model) && MessagesPigeonInternal.deepEquals(lhs.osVersion, rhs.osVersion)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("DeviceIdentity")
    MessagesPigeonInternal.deepHash(value: make, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: model, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: osVersion, hasher: &hasher)
  }

  public var description: String {
    return "DeviceIdentity(make: \(String(describing: make)), model: \(String(describing: model)), osVersion: \(String(describing: osVersion)))"
  }
}

/// What the metering pre-sweep settled on, and how firmly.
///
/// Generated class from Pigeon that represents data sent in messages.
struct MeteringResult: Hashable, CustomStringConvertible {
  /// The exposure locked for the whole session.
  var exposureTimeNs: Int64
  var iso: Int64
  var colorTemperatureK: Int64
  /// Locked focus, in diopters. §2.3: near the hyperfocal distance — locking
  /// to infinity softens near objects, locking to macro ruins everything else.
  var focusDistanceDiopters: Double
  var lockQuality: PlatformLockQuality
  /// How many frames the sweep observed. A sweep that saw three frames did not
  /// see the sphere, and its percentile means nothing.
  var sampleCount: Int64
  /// The EV actually locked, relative to the sweep's own median frame.
  var chosenEv: Double
  /// Reported alongside [percentile65Ev] so the §2.3 decision is *visible* in
  /// the data rather than only in the code: interiors are mid-tone with a few
  /// bright windows, and the mean is dragged bright by the windows and crushes
  /// the interior.
  var meanEv: Double
  var percentile65Ev: Double
  var aeConverged: Bool
  /// Whether `NOISE_REDUCTION` / `EDGE` / `TONEMAP` / `COLOR_CORRECTION` were
  /// pinned to fixed modes (§2.3 step 5). Easy to miss, and leaving any of
  /// them adaptive reintroduces exactly the per-frame photometric
  /// inconsistency the AE lock exists to remove.
  var pinnedProcessingModes: Bool
  var note: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> MeteringResult? {
    let exposureTimeNs = pigeonVar_list[0] as! Int64
    let iso = pigeonVar_list[1] as! Int64
    let colorTemperatureK = pigeonVar_list[2] as! Int64
    let focusDistanceDiopters = pigeonVar_list[3] as! Double
    let lockQuality = pigeonVar_list[4] as! PlatformLockQuality
    let sampleCount = pigeonVar_list[5] as! Int64
    let chosenEv = pigeonVar_list[6] as! Double
    let meanEv = pigeonVar_list[7] as! Double
    let percentile65Ev = pigeonVar_list[8] as! Double
    let aeConverged = pigeonVar_list[9] as! Bool
    let pinnedProcessingModes = pigeonVar_list[10] as! Bool
    let note: String? = nilOrValue(pigeonVar_list[11])

    return MeteringResult(
      exposureTimeNs: exposureTimeNs,
      iso: iso,
      colorTemperatureK: colorTemperatureK,
      focusDistanceDiopters: focusDistanceDiopters,
      lockQuality: lockQuality,
      sampleCount: sampleCount,
      chosenEv: chosenEv,
      meanEv: meanEv,
      percentile65Ev: percentile65Ev,
      aeConverged: aeConverged,
      pinnedProcessingModes: pinnedProcessingModes,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      exposureTimeNs,
      iso,
      colorTemperatureK,
      focusDistanceDiopters,
      lockQuality,
      sampleCount,
      chosenEv,
      meanEv,
      percentile65Ev,
      aeConverged,
      pinnedProcessingModes,
      note,
    ]
  }
  static func == (lhs: MeteringResult, rhs: MeteringResult) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.exposureTimeNs, rhs.exposureTimeNs) && MessagesPigeonInternal.deepEquals(lhs.iso, rhs.iso) && MessagesPigeonInternal.deepEquals(lhs.colorTemperatureK, rhs.colorTemperatureK) && MessagesPigeonInternal.deepEquals(lhs.focusDistanceDiopters, rhs.focusDistanceDiopters) && MessagesPigeonInternal.deepEquals(lhs.lockQuality, rhs.lockQuality) && MessagesPigeonInternal.deepEquals(lhs.sampleCount, rhs.sampleCount) && MessagesPigeonInternal.deepEquals(lhs.chosenEv, rhs.chosenEv) && MessagesPigeonInternal.deepEquals(lhs.meanEv, rhs.meanEv) && MessagesPigeonInternal.deepEquals(lhs.percentile65Ev, rhs.percentile65Ev) && MessagesPigeonInternal.deepEquals(lhs.aeConverged, rhs.aeConverged) && MessagesPigeonInternal.deepEquals(lhs.pinnedProcessingModes, rhs.pinnedProcessingModes) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("MeteringResult")
    MessagesPigeonInternal.deepHash(value: exposureTimeNs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: iso, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: colorTemperatureK, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: focusDistanceDiopters, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: lockQuality, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: sampleCount, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: chosenEv, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: meanEv, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: percentile65Ev, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: aeConverged, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: pinnedProcessingModes, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "MeteringResult(exposureTimeNs: \(String(describing: exposureTimeNs)), iso: \(String(describing: iso)), colorTemperatureK: \(String(describing: colorTemperatureK)), focusDistanceDiopters: \(String(describing: focusDistanceDiopters)), lockQuality: \(String(describing: lockQuality)), sampleCount: \(String(describing: sampleCount)), chosenEv: \(String(describing: chosenEv)), meanEv: \(String(describing: meanEv)), percentile65Ev: \(String(describing: percentile65Ev)), aeConverged: \(String(describing: aeConverged)), pinnedProcessingModes: \(String(describing: pinnedProcessingModes)), note: \(String(describing: note)))"
  }
}

/// One frame returned by a bracketed capture.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PlatformFrame: Hashable, CustomStringConvertible {
  var filePath: String
  /// The bias in stops this frame was *requested* at.
  var evBias: Double
  /// Shutter instant, already converted to the single monotonic microsecond
  /// base the pose stream shares (§2.5, §3.5).
  var timestampUs: Int64
  var byteCount: Int64
  /// What the sensor actually did, as opposed to what was asked for. When a
  /// bracket comes back with two identical frames — the failure R3 warns about
  /// on devices without real bracketing — this is the only evidence of it.
  var exposureTimeNs: Int64? = nil
  var iso: Int64? = nil
  /// Computed from the *actual* exposure and ISO, so the "EV separation within
  /// 0.3 EV of requested" check is against reality rather than intent.
  var achievedEvBias: Double? = nil
  /// Centre-crop channel means, present only when
  /// [CaptureFormatRequest.computeFrameStatistics] is set. The grey-card
  /// AE/AWB lock measurement (§6).
  var meanR: Double? = nil
  var meanG: Double? = nil
  var meanB: Double? = nil
  var note: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PlatformFrame? {
    let filePath = pigeonVar_list[0] as! String
    let evBias = pigeonVar_list[1] as! Double
    let timestampUs = pigeonVar_list[2] as! Int64
    let byteCount = pigeonVar_list[3] as! Int64
    let exposureTimeNs: Int64? = nilOrValue(pigeonVar_list[4])
    let iso: Int64? = nilOrValue(pigeonVar_list[5])
    let achievedEvBias: Double? = nilOrValue(pigeonVar_list[6])
    let meanR: Double? = nilOrValue(pigeonVar_list[7])
    let meanG: Double? = nilOrValue(pigeonVar_list[8])
    let meanB: Double? = nilOrValue(pigeonVar_list[9])
    let note: String? = nilOrValue(pigeonVar_list[10])

    return PlatformFrame(
      filePath: filePath,
      evBias: evBias,
      timestampUs: timestampUs,
      byteCount: byteCount,
      exposureTimeNs: exposureTimeNs,
      iso: iso,
      achievedEvBias: achievedEvBias,
      meanR: meanR,
      meanG: meanG,
      meanB: meanB,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      filePath,
      evBias,
      timestampUs,
      byteCount,
      exposureTimeNs,
      iso,
      achievedEvBias,
      meanR,
      meanG,
      meanB,
      note,
    ]
  }
  static func == (lhs: PlatformFrame, rhs: PlatformFrame) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.filePath, rhs.filePath) && MessagesPigeonInternal.deepEquals(lhs.evBias, rhs.evBias) && MessagesPigeonInternal.deepEquals(lhs.timestampUs, rhs.timestampUs) && MessagesPigeonInternal.deepEquals(lhs.byteCount, rhs.byteCount) && MessagesPigeonInternal.deepEquals(lhs.exposureTimeNs, rhs.exposureTimeNs) && MessagesPigeonInternal.deepEquals(lhs.iso, rhs.iso) && MessagesPigeonInternal.deepEquals(lhs.achievedEvBias, rhs.achievedEvBias) && MessagesPigeonInternal.deepEquals(lhs.meanR, rhs.meanR) && MessagesPigeonInternal.deepEquals(lhs.meanG, rhs.meanG) && MessagesPigeonInternal.deepEquals(lhs.meanB, rhs.meanB) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PlatformFrame")
    MessagesPigeonInternal.deepHash(value: filePath, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: evBias, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: timestampUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: byteCount, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: exposureTimeNs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: iso, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: achievedEvBias, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: meanR, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: meanG, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: meanB, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "PlatformFrame(filePath: \(String(describing: filePath)), evBias: \(String(describing: evBias)), timestampUs: \(String(describing: timestampUs)), byteCount: \(String(describing: byteCount)), exposureTimeNs: \(String(describing: exposureTimeNs)), iso: \(String(describing: iso)), achievedEvBias: \(String(describing: achievedEvBias)), meanR: \(String(describing: meanR)), meanG: \(String(describing: meanG)), meanB: \(String(describing: meanB)), note: \(String(describing: note)))"
  }
}

/// One bracket's worth of frames plus the timing the 600 ms budget is judged
/// against.
///
/// Generated class from Pigeon that represents data sent in messages.
struct CaptureResponse: Hashable, CustomStringConvertible {
  var frames: [PlatformFrame]
  /// Trigger → last frame's **pixels in hand**, not `onCaptureCompleted`.
  /// Metadata routinely completes well before pixels are delivered, and pixels
  /// are what Phase 05 needs; timing the metadata would give a flattering,
  /// useless number (Spike C).
  var burstWallClockMs: Double
  /// From sensor timestamps: what the sensor sustains, separated from what
  /// processing then costs. That distinction is what the JPEG-vs-YUV decision
  /// turns on (R3 §9).
  var shutterToShutterMs: [Double]
  var mode: PlatformBracketMode
  /// Clamping is reported, never silent: a silently clamped bracket looks like
  /// a passing capture while delivering less dynamic range than assumed.
  var clampedExposure: Bool
  var clampedIso: Bool
  /// What the YUV path moved *off* the hot path — not what it removed.
  var deferredEncodeMs: Double
  var note: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> CaptureResponse? {
    let frames = pigeonVar_list[0] as! [PlatformFrame]
    let burstWallClockMs = pigeonVar_list[1] as! Double
    let shutterToShutterMs = pigeonVar_list[2] as! [Double]
    let mode = pigeonVar_list[3] as! PlatformBracketMode
    let clampedExposure = pigeonVar_list[4] as! Bool
    let clampedIso = pigeonVar_list[5] as! Bool
    let deferredEncodeMs = pigeonVar_list[6] as! Double
    let note: String? = nilOrValue(pigeonVar_list[7])

    return CaptureResponse(
      frames: frames,
      burstWallClockMs: burstWallClockMs,
      shutterToShutterMs: shutterToShutterMs,
      mode: mode,
      clampedExposure: clampedExposure,
      clampedIso: clampedIso,
      deferredEncodeMs: deferredEncodeMs,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      frames,
      burstWallClockMs,
      shutterToShutterMs,
      mode,
      clampedExposure,
      clampedIso,
      deferredEncodeMs,
      note,
    ]
  }
  static func == (lhs: CaptureResponse, rhs: CaptureResponse) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.frames, rhs.frames) && MessagesPigeonInternal.deepEquals(lhs.burstWallClockMs, rhs.burstWallClockMs) && MessagesPigeonInternal.deepEquals(lhs.shutterToShutterMs, rhs.shutterToShutterMs) && MessagesPigeonInternal.deepEquals(lhs.mode, rhs.mode) && MessagesPigeonInternal.deepEquals(lhs.clampedExposure, rhs.clampedExposure) && MessagesPigeonInternal.deepEquals(lhs.clampedIso, rhs.clampedIso) && MessagesPigeonInternal.deepEquals(lhs.deferredEncodeMs, rhs.deferredEncodeMs) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("CaptureResponse")
    MessagesPigeonInternal.deepHash(value: frames, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: burstWallClockMs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: shutterToShutterMs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: mode, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: clampedExposure, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: clampedIso, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: deferredEncodeMs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "CaptureResponse(frames: \(String(describing: frames)), burstWallClockMs: \(String(describing: burstWallClockMs)), shutterToShutterMs: \(String(describing: shutterToShutterMs)), mode: \(String(describing: mode)), clampedExposure: \(String(describing: clampedExposure)), clampedIso: \(String(describing: clampedIso)), deferredEncodeMs: \(String(describing: deferredEncodeMs)), note: \(String(describing: note)))"
  }
}

/// What this device can actually do, and — when it cannot — why.
///
/// Exists so a gyro-less tablet is refused at the feature entry point with a
/// sentence a site manager can act on, rather than producing a panorama built
/// on accelerometer tilt alone (Phase 07 §6 pitfall 3, Phase 12 §1).
///
/// Generated class from Pigeon that represents data sent in messages.
struct PoseCapabilities: Hashable, CustomStringConvertible {
  /// The one hard requirement. Without it there is no attitude source that
  /// tracks a pan, and the pipeline cannot work at all.
  var hasGyroscope: Bool
  /// Needed for the gravity lock that makes pitch and roll drift-free.
  var hasAccelerometer: Bool
  /// Whether the fused, magnetometer-free attitude sensor is present —
  /// `TYPE_GAME_ROTATION_VECTOR` on Android, `isDeviceMotionAvailable` on iOS.
  var hasFusedRotation: Bool
  /// Whether a fused gravity vector is available (`TYPE_GRAVITY` /
  /// `CMDeviceMotion.gravity`). Its absence is recoverable — raw accelerometer
  /// would do — but it is reported because [PlatformPoseSample.upX] is what the
  /// levelling step of Math §7 ultimately rests on.
  var hasGravity: Bool
  /// **Must be false.** Reported rather than assumed so that a build which
  /// accidentally selected `TYPE_ROTATION_VECTOR` or `.xTrueNorthZVertical`
  /// shows up as a recorded fact in the bundle instead of as an unexplained
  /// heading error on a site with steel in the walls.
  var usesMagnetometer: Bool
  /// The frame [PlatformPoseSample]'s quaternion is expressed against.
  var frame: PlatformPoseFrame
  /// The fastest the attitude source will run, in µs between samples.
  /// `Sensor.getMinDelay()` on Android; 0 where the platform does not say.
  var minDelayUs: Int64
  /// Plain-language description of what was found, for the bundle and the
  /// report.
  var detail: String
  /// Non-null exactly when this device cannot be supported, phrased as
  /// something the user can act on.
  var unsupportedReason: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PoseCapabilities? {
    let hasGyroscope = pigeonVar_list[0] as! Bool
    let hasAccelerometer = pigeonVar_list[1] as! Bool
    let hasFusedRotation = pigeonVar_list[2] as! Bool
    let hasGravity = pigeonVar_list[3] as! Bool
    let usesMagnetometer = pigeonVar_list[4] as! Bool
    let frame = pigeonVar_list[5] as! PlatformPoseFrame
    let minDelayUs = pigeonVar_list[6] as! Int64
    let detail = pigeonVar_list[7] as! String
    let unsupportedReason: String? = nilOrValue(pigeonVar_list[8])

    return PoseCapabilities(
      hasGyroscope: hasGyroscope,
      hasAccelerometer: hasAccelerometer,
      hasFusedRotation: hasFusedRotation,
      hasGravity: hasGravity,
      usesMagnetometer: usesMagnetometer,
      frame: frame,
      minDelayUs: minDelayUs,
      detail: detail,
      unsupportedReason: unsupportedReason
    )
  }
  func toList() -> [Any?] {
    return [
      hasGyroscope,
      hasAccelerometer,
      hasFusedRotation,
      hasGravity,
      usesMagnetometer,
      frame,
      minDelayUs,
      detail,
      unsupportedReason,
    ]
  }
  static func == (lhs: PoseCapabilities, rhs: PoseCapabilities) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.hasGyroscope, rhs.hasGyroscope) && MessagesPigeonInternal.deepEquals(lhs.hasAccelerometer, rhs.hasAccelerometer) && MessagesPigeonInternal.deepEquals(lhs.hasFusedRotation, rhs.hasFusedRotation) && MessagesPigeonInternal.deepEquals(lhs.hasGravity, rhs.hasGravity) && MessagesPigeonInternal.deepEquals(lhs.usesMagnetometer, rhs.usesMagnetometer) && MessagesPigeonInternal.deepEquals(lhs.frame, rhs.frame) && MessagesPigeonInternal.deepEquals(lhs.minDelayUs, rhs.minDelayUs) && MessagesPigeonInternal.deepEquals(lhs.detail, rhs.detail) && MessagesPigeonInternal.deepEquals(lhs.unsupportedReason, rhs.unsupportedReason)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PoseCapabilities")
    MessagesPigeonInternal.deepHash(value: hasGyroscope, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hasAccelerometer, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hasFusedRotation, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: hasGravity, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: usesMagnetometer, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: frame, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: minDelayUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: detail, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: unsupportedReason, hasher: &hasher)
  }

  public var description: String {
    return "PoseCapabilities(hasGyroscope: \(String(describing: hasGyroscope)), hasAccelerometer: \(String(describing: hasAccelerometer)), hasFusedRotation: \(String(describing: hasFusedRotation)), hasGravity: \(String(describing: hasGravity)), usesMagnetometer: \(String(describing: usesMagnetometer)), frame: \(String(describing: frame)), minDelayUs: \(String(describing: minDelayUs)), detail: \(String(describing: detail)), unsupportedReason: \(String(describing: unsupportedReason)))"
  }
}

/// How pose timestamps were reconciled with the clock Phase 06 puts shutter
/// timestamps on.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PoseClockInfo: Hashable, CustomStringConvertible {
  var base: PlatformPoseClockBase
  /// Added to a raw sample timestamp to land on the shared motion clock.
  /// Exactly `0` for [PlatformPoseClockBase.androidElapsedRealtime] and
  /// [PlatformPoseClockBase.iosSystemUptime], where the two *are* one clock.
  var offsetUs: Int64
  /// Bound on the offset estimate. Zero when nothing needed estimating.
  var uncertaintyUs: Int64
  var note: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PoseClockInfo? {
    let base = pigeonVar_list[0] as! PlatformPoseClockBase
    let offsetUs = pigeonVar_list[1] as! Int64
    let uncertaintyUs = pigeonVar_list[2] as! Int64
    let note = pigeonVar_list[3] as! String

    return PoseClockInfo(
      base: base,
      offsetUs: offsetUs,
      uncertaintyUs: uncertaintyUs,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      base,
      offsetUs,
      uncertaintyUs,
      note,
    ]
  }
  static func == (lhs: PoseClockInfo, rhs: PoseClockInfo) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.base, rhs.base) && MessagesPigeonInternal.deepEquals(lhs.offsetUs, rhs.offsetUs) && MessagesPigeonInternal.deepEquals(lhs.uncertaintyUs, rhs.uncertaintyUs) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PoseClockInfo")
    MessagesPigeonInternal.deepHash(value: base, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: offsetUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: uncertaintyUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "PoseClockInfo(base: \(String(describing: base)), offsetUs: \(String(describing: offsetUs)), uncertaintyUs: \(String(describing: uncertaintyUs)), note: \(String(describing: note)))"
  }
}

/// What starting the stream settled.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PoseStreamInfo: Hashable, CustomStringConvertible {
  var frame: PlatformPoseFrame
  var clock: PoseClockInfo
  /// The period actually requested of the platform (§4 asks for 10 000 µs).
  var samplingPeriodUs: Int64
  /// The floor the platform imposes on it; 0 when unreported.
  var minDelayUs: Int64
  var note: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PoseStreamInfo? {
    let frame = pigeonVar_list[0] as! PlatformPoseFrame
    let clock = pigeonVar_list[1] as! PoseClockInfo
    let samplingPeriodUs = pigeonVar_list[2] as! Int64
    let minDelayUs = pigeonVar_list[3] as! Int64
    let note = pigeonVar_list[4] as! String

    return PoseStreamInfo(
      frame: frame,
      clock: clock,
      samplingPeriodUs: samplingPeriodUs,
      minDelayUs: minDelayUs,
      note: note
    )
  }
  func toList() -> [Any?] {
    return [
      frame,
      clock,
      samplingPeriodUs,
      minDelayUs,
      note,
    ]
  }
  static func == (lhs: PoseStreamInfo, rhs: PoseStreamInfo) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.frame, rhs.frame) && MessagesPigeonInternal.deepEquals(lhs.clock, rhs.clock) && MessagesPigeonInternal.deepEquals(lhs.samplingPeriodUs, rhs.samplingPeriodUs) && MessagesPigeonInternal.deepEquals(lhs.minDelayUs, rhs.minDelayUs) && MessagesPigeonInternal.deepEquals(lhs.note, rhs.note)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PoseStreamInfo")
    MessagesPigeonInternal.deepHash(value: frame, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: clock, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: samplingPeriodUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: minDelayUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: note, hasher: &hasher)
  }

  public var description: String {
    return "PoseStreamInfo(frame: \(String(describing: frame)), clock: \(String(describing: clock)), samplingPeriodUs: \(String(describing: samplingPeriodUs)), minDelayUs: \(String(describing: minDelayUs)), note: \(String(describing: note)))"
  }
}

/// One attitude sample, exactly as the platform reported it.
///
/// **No frame conversion happens on the native side.** This is the same
/// decision the intrinsics chain makes for the same reason, one step further:
/// Phase 07 §2 says the world→world conversion "must be proven by the §5 test,
/// not by derivation", and a conversion that lives in Kotlin and Swift can only
/// ever be tested on the two devices in the room. In Dart it is one function,
/// pinned by unit tests over every branch, and the device test then confirms
/// the platform docs rather than the arithmetic.
///
/// Generated class from Pigeon that represents data sent in messages.
struct PlatformPoseSample: Hashable, CustomStringConvertible {
  /// Device→reference rotation, in the frame named by [PoseStreamInfo.frame].
  var qx: Double
  var qy: Double
  var qz: Double
  var qw: Double
  /// A **unit** vector in the *device* frame pointing **away from the earth**.
  ///
  /// Normalised natively rather than passed raw because the two platforms
  /// disagree on both magnitude and sign: Android `TYPE_GRAVITY` reads
  /// `(0, 0, +9.81)` with the device flat on its back, iOS
  /// `CMDeviceMotion.gravity` reads `(0, 0, −1)` in the same pose. Reconciling
  /// that is a one-line, per-platform fact about what a sensor reads in a known
  /// physical pose — which is precisely the kind of thing the native side is
  /// here to report — and it leaves Dart with one convention instead of two.
  ///
  /// Dart does **not** take it on trust: `PlatformAhrsPoseSource` checks that
  /// this vector, rotated into the world frame, actually comes out pointing up,
  /// and refuses to start if it does not (Phase 07 §2's reflection hazard, in
  /// its detectable half).
  var upX: Double
  var upY: Double
  var upZ: Double
  /// `|ω|` from the gyroscope, rad/s. A magnitude rather than a vector because
  /// it is frame-invariant, which keeps one more sign convention out of the
  /// wire format — and the steadiness gate only ever wants the magnitude.
  var angularSpeedRadPerSec: Double
  /// Sample instant on the shared motion clock, already converted by
  /// [PoseClockInfo.offsetUs].
  var timestampUs: Int64
  /// Monotonic counter from the start of the stream, so a gap in delivery is
  /// distinguishable from a gap in sampling.
  var sequence: Int64
  /// Android `SensorEvent.accuracy` (`SENSOR_STATUS_*`); `-1` on iOS, which
  /// publishes no equivalent.
  var accuracy: Int64


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> PlatformPoseSample? {
    let qx = pigeonVar_list[0] as! Double
    let qy = pigeonVar_list[1] as! Double
    let qz = pigeonVar_list[2] as! Double
    let qw = pigeonVar_list[3] as! Double
    let upX = pigeonVar_list[4] as! Double
    let upY = pigeonVar_list[5] as! Double
    let upZ = pigeonVar_list[6] as! Double
    let angularSpeedRadPerSec = pigeonVar_list[7] as! Double
    let timestampUs = pigeonVar_list[8] as! Int64
    let sequence = pigeonVar_list[9] as! Int64
    let accuracy = pigeonVar_list[10] as! Int64

    return PlatformPoseSample(
      qx: qx,
      qy: qy,
      qz: qz,
      qw: qw,
      upX: upX,
      upY: upY,
      upZ: upZ,
      angularSpeedRadPerSec: angularSpeedRadPerSec,
      timestampUs: timestampUs,
      sequence: sequence,
      accuracy: accuracy
    )
  }
  func toList() -> [Any?] {
    return [
      qx,
      qy,
      qz,
      qw,
      upX,
      upY,
      upZ,
      angularSpeedRadPerSec,
      timestampUs,
      sequence,
      accuracy,
    ]
  }
  static func == (lhs: PlatformPoseSample, rhs: PlatformPoseSample) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return MessagesPigeonInternal.deepEquals(lhs.qx, rhs.qx) && MessagesPigeonInternal.deepEquals(lhs.qy, rhs.qy) && MessagesPigeonInternal.deepEquals(lhs.qz, rhs.qz) && MessagesPigeonInternal.deepEquals(lhs.qw, rhs.qw) && MessagesPigeonInternal.deepEquals(lhs.upX, rhs.upX) && MessagesPigeonInternal.deepEquals(lhs.upY, rhs.upY) && MessagesPigeonInternal.deepEquals(lhs.upZ, rhs.upZ) && MessagesPigeonInternal.deepEquals(lhs.angularSpeedRadPerSec, rhs.angularSpeedRadPerSec) && MessagesPigeonInternal.deepEquals(lhs.timestampUs, rhs.timestampUs) && MessagesPigeonInternal.deepEquals(lhs.sequence, rhs.sequence) && MessagesPigeonInternal.deepEquals(lhs.accuracy, rhs.accuracy)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("PlatformPoseSample")
    MessagesPigeonInternal.deepHash(value: qx, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: qy, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: qz, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: qw, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: upX, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: upY, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: upZ, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: angularSpeedRadPerSec, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: timestampUs, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: sequence, hasher: &hasher)
    MessagesPigeonInternal.deepHash(value: accuracy, hasher: &hasher)
  }

  public var description: String {
    return "PlatformPoseSample(qx: \(String(describing: qx)), qy: \(String(describing: qy)), qz: \(String(describing: qz)), qw: \(String(describing: qw)), upX: \(String(describing: upX)), upY: \(String(describing: upY)), upZ: \(String(describing: upZ)), angularSpeedRadPerSec: \(String(describing: angularSpeedRadPerSec)), timestampUs: \(String(describing: timestampUs)), sequence: \(String(describing: sequence)), accuracy: \(String(describing: accuracy)))"
  }
}

private class MessagesPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformCameraFacing(rawValue: enumResultAsInt)
      }
      return nil
    case 130:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformThermalState(rawValue: enumResultAsInt)
      }
      return nil
    case 131:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformLockQuality(rawValue: enumResultAsInt)
      }
      return nil
    case 132:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformTimestampBase(rawValue: enumResultAsInt)
      }
      return nil
    case 133:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformCaptureFormat(rawValue: enumResultAsInt)
      }
      return nil
    case 134:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformBracketMode(rawValue: enumResultAsInt)
      }
      return nil
    case 135:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformHardwareLevel(rawValue: enumResultAsInt)
      }
      return nil
    case 136:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformPoseFrame(rawValue: enumResultAsInt)
      }
      return nil
    case 137:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return PlatformPoseClockBase(rawValue: enumResultAsInt)
      }
      return nil
    case 138:
      return PlatformSize.fromList(self.readValue() as! [Any?])
    case 139:
      return PlatformRect.fromList(self.readValue() as! [Any?])
    case 140:
      return CameraDescriptor.fromList(self.readValue() as! [Any?])
    case 141:
      return CaptureFormatRequest.fromList(self.readValue() as! [Any?])
    case 142:
      return AndroidIntrinsicFacts.fromList(self.readValue() as! [Any?])
    case 143:
      return IosIntrinsicFacts.fromList(self.readValue() as! [Any?])
    case 144:
      return ClockSyncInfo.fromList(self.readValue() as! [Any?])
    case 145:
      return ClockOffsetSample.fromList(self.readValue() as! [Any?])
    case 146:
      return CameraOpenResult.fromList(self.readValue() as! [Any?])
    case 147:
      return DeviceIdentity.fromList(self.readValue() as! [Any?])
    case 148:
      return MeteringResult.fromList(self.readValue() as! [Any?])
    case 149:
      return PlatformFrame.fromList(self.readValue() as! [Any?])
    case 150:
      return CaptureResponse.fromList(self.readValue() as! [Any?])
    case 151:
      return PoseCapabilities.fromList(self.readValue() as! [Any?])
    case 152:
      return PoseClockInfo.fromList(self.readValue() as! [Any?])
    case 153:
      return PoseStreamInfo.fromList(self.readValue() as! [Any?])
    case 154:
      return PlatformPoseSample.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class MessagesPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? PlatformCameraFacing {
      super.writeByte(129)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformThermalState {
      super.writeByte(130)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformLockQuality {
      super.writeByte(131)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformTimestampBase {
      super.writeByte(132)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformCaptureFormat {
      super.writeByte(133)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformBracketMode {
      super.writeByte(134)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformHardwareLevel {
      super.writeByte(135)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformPoseFrame {
      super.writeByte(136)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformPoseClockBase {
      super.writeByte(137)
      super.writeValue(value.rawValue)
    } else if let value = value as? PlatformSize {
      super.writeByte(138)
      super.writeValue(value.toList())
    } else if let value = value as? PlatformRect {
      super.writeByte(139)
      super.writeValue(value.toList())
    } else if let value = value as? CameraDescriptor {
      super.writeByte(140)
      super.writeValue(value.toList())
    } else if let value = value as? CaptureFormatRequest {
      super.writeByte(141)
      super.writeValue(value.toList())
    } else if let value = value as? AndroidIntrinsicFacts {
      super.writeByte(142)
      super.writeValue(value.toList())
    } else if let value = value as? IosIntrinsicFacts {
      super.writeByte(143)
      super.writeValue(value.toList())
    } else if let value = value as? ClockSyncInfo {
      super.writeByte(144)
      super.writeValue(value.toList())
    } else if let value = value as? ClockOffsetSample {
      super.writeByte(145)
      super.writeValue(value.toList())
    } else if let value = value as? CameraOpenResult {
      super.writeByte(146)
      super.writeValue(value.toList())
    } else if let value = value as? DeviceIdentity {
      super.writeByte(147)
      super.writeValue(value.toList())
    } else if let value = value as? MeteringResult {
      super.writeByte(148)
      super.writeValue(value.toList())
    } else if let value = value as? PlatformFrame {
      super.writeByte(149)
      super.writeValue(value.toList())
    } else if let value = value as? CaptureResponse {
      super.writeByte(150)
      super.writeValue(value.toList())
    } else if let value = value as? PoseCapabilities {
      super.writeByte(151)
      super.writeValue(value.toList())
    } else if let value = value as? PoseClockInfo {
      super.writeByte(152)
      super.writeValue(value.toList())
    } else if let value = value as? PoseStreamInfo {
      super.writeByte(153)
      super.writeValue(value.toList())
    } else if let value = value as? PlatformPoseSample {
      super.writeByte(154)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class MessagesPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return MessagesPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return MessagesPigeonCodecWriter(data: data)
  }
}

class MessagesPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = MessagesPigeonCodec(readerWriter: MessagesPigeonCodecReaderWriter())
}


/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol SphereCameraHostApi {
  /// Enumerates the physical cameras and their capabilities.
  func listCameras(completion: @escaping (Result<[CameraDescriptor], Error>) -> Void)
  /// Opens [cameraId] and configures the capture and preview streams.
  func open(cameraId: String, format: CaptureFormatRequest, completion: @escaping (Result<CameraOpenResult, Error>) -> Void)
  /// Attaches the preview and returns the Flutter texture id. Preview is
  /// delivered through the platform texture APIs; bytes are never streamed
  /// over the channel (§4).
  func attachPreview(completion: @escaping (Result<Int64, Error>) -> Void)
  /// Releases the preview texture without closing the camera.
  func detachPreview(completion: @escaping (Result<Void, Error>) -> Void)
  /// Runs the metering pre-sweep for [durationSeconds], then hard-locks AE,
  /// AWB and AF for the rest of the session.
  func meterAndLock(durationSeconds: Double, completion: @escaping (Result<MeteringResult, Error>) -> Void)
  /// Releases the lock.
  func unlock(completion: @escaping (Result<Void, Error>) -> Void)
  /// Fires one bracket at [evBiases], writing frames into [outputDirectory]
  /// with names beginning [namePrefix].
  func captureBracket(evBiases: [Double], outputDirectory: String, namePrefix: String, completion: @escaping (Result<CaptureResponse, Error>) -> Void)
  /// Current thermal state (§5).
  func thermalState(completion: @escaping (Result<PlatformThermalState, Error>) -> Void)
  /// Re-measures the camera↔motion clock offset. Called repeatedly by the §6
  /// stability test; cheap enough to call in a loop.
  func sampleClockOffset(completion: @escaping (Result<ClockOffsetSample, Error>) -> Void)
  /// Make, model and OS version, for the output's EXIF (Phase 11 §2).
  ///
  /// On the camera API rather than the pose one because it is asked once, at
  /// open time, alongside every other fact about the hardware that took the
  /// frames.
  func deviceIdentity(completion: @escaping (Result<DeviceIdentity, Error>) -> Void)
  /// The largest texture edge this GPU will accept, in pixels (Phase 11 §3.1).
  ///
  /// The viewer needs this before it uploads a panorama, and it is the one
  /// number that decides whether the sphere renders or comes up **black**.
  /// Many mid-range tablet GPUs cap at 4096, and an 8192-wide upload against
  /// such a cap does not raise an error — it silently produces nothing, which
  /// looks exactly like a stitcher that emitted an empty image.
  ///
  /// Returns 0 when the platform cannot say, which the caller must read as
  /// "assume the conservative floor" rather than "no limit".
  func maxTextureSize(completion: @escaping (Result<Int64, Error>) -> Void)
  /// Total physical RAM in MB, for the memory-tier probe (arch §6.5).
  func totalPhysicalMemoryMb(completion: @escaping (Result<Int64, Error>) -> Void)
  /// How much more memory *this process* may allocate, in MB, or `-1` where
  /// the platform will not say.
  ///
  /// Not the tier decision — that is [totalPhysicalMemoryMb], and it is total
  /// rather than available on purpose (Phase 10 §4): available fluctuates, and
  /// a tier that moves with it would make output resolution differ between two
  /// runs on the same device, which makes a bug report useless.
  ///
  /// This is the pre-flight check instead. iOS answers it with
  /// `os_proc_available_memory()`, and it matters there specifically because
  /// iOS kills a process on memory pressure with no recoverable signal — no
  /// `bad_alloc` to catch, no chance to drop a tier. So the only defence is to
  /// ask before starting, and to start one tier lower if the answer is smaller
  /// than the tier needs. Android returns `-1`: it has no per-process
  /// equivalent, and a real `OutOfMemoryError` there *is* catchable, so the
  /// retry path covers it.
  func availableProcessMemoryMb(completion: @escaping (Result<Int64, Error>) -> Void)
  /// Battery charge as a whole percentage, or `-1` where the platform will not
  /// say.
  ///
  /// Phase 12 §3 asks for the drain **per station** to be measured and
  /// published, because a site walk is up to thirty of them and a tablet that
  /// cannot finish one is a feature nobody can use. It is a percentage rather
  /// than a mAh figure on purpose: percentage is what both platforms report
  /// without a vendor API, and the number that matters is "how many stations
  /// does this tablet have left", which is a percentage divided by a
  /// percentage.
  ///
  /// It is coarse — most devices report in steps of 1% and some in steps of 5%
  /// — so one station's drain is at or below the quantisation. The device matrix
  /// therefore measures across a *run* of stations and divides, which is also
  /// the number a site lead actually wants.
  func batteryPercent(completion: @escaping (Result<Int64, Error>) -> Void)
  /// Closes the camera and releases every resource.
  func close(completion: @escaping (Result<Void, Error>) -> Void)
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class SphereCameraHostApiSetup {
  static var codec: FlutterStandardMessageCodec { MessagesPigeonCodec.shared }
  /// Sets up an instance of `SphereCameraHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: SphereCameraHostApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// Enumerates the physical cameras and their capabilities.
    let listCamerasChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.listCameras\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      listCamerasChannel.setMessageHandler { _, reply in
        api.listCameras { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      listCamerasChannel.setMessageHandler(nil)
    }
    /// Opens [cameraId] and configures the capture and preview streams.
    let openChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.open\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      openChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let cameraIdArg = args[0] as! String
        let formatArg = args[1] as! CaptureFormatRequest
        api.open(cameraId: cameraIdArg, format: formatArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      openChannel.setMessageHandler(nil)
    }
    /// Attaches the preview and returns the Flutter texture id. Preview is
    /// delivered through the platform texture APIs; bytes are never streamed
    /// over the channel (§4).
    let attachPreviewChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.attachPreview\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      attachPreviewChannel.setMessageHandler { _, reply in
        api.attachPreview { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      attachPreviewChannel.setMessageHandler(nil)
    }
    /// Releases the preview texture without closing the camera.
    let detachPreviewChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.detachPreview\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      detachPreviewChannel.setMessageHandler { _, reply in
        api.detachPreview { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      detachPreviewChannel.setMessageHandler(nil)
    }
    /// Runs the metering pre-sweep for [durationSeconds], then hard-locks AE,
    /// AWB and AF for the rest of the session.
    let meterAndLockChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.meterAndLock\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      meterAndLockChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let durationSecondsArg = args[0] as! Double
        api.meterAndLock(durationSeconds: durationSecondsArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      meterAndLockChannel.setMessageHandler(nil)
    }
    /// Releases the lock.
    let unlockChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.unlock\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      unlockChannel.setMessageHandler { _, reply in
        api.unlock { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      unlockChannel.setMessageHandler(nil)
    }
    /// Fires one bracket at [evBiases], writing frames into [outputDirectory]
    /// with names beginning [namePrefix].
    let captureBracketChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.captureBracket\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      captureBracketChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let evBiasesArg = args[0] as! [Double]
        let outputDirectoryArg = args[1] as! String
        let namePrefixArg = args[2] as! String
        api.captureBracket(evBiases: evBiasesArg, outputDirectory: outputDirectoryArg, namePrefix: namePrefixArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      captureBracketChannel.setMessageHandler(nil)
    }
    /// Current thermal state (§5).
    let thermalStateChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.thermalState\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      thermalStateChannel.setMessageHandler { _, reply in
        api.thermalState { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      thermalStateChannel.setMessageHandler(nil)
    }
    /// Re-measures the camera↔motion clock offset. Called repeatedly by the §6
    /// stability test; cheap enough to call in a loop.
    let sampleClockOffsetChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.sampleClockOffset\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      sampleClockOffsetChannel.setMessageHandler { _, reply in
        api.sampleClockOffset { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      sampleClockOffsetChannel.setMessageHandler(nil)
    }
    /// Make, model and OS version, for the output's EXIF (Phase 11 §2).
    ///
    /// On the camera API rather than the pose one because it is asked once, at
    /// open time, alongside every other fact about the hardware that took the
    /// frames.
    let deviceIdentityChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.deviceIdentity\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      deviceIdentityChannel.setMessageHandler { _, reply in
        api.deviceIdentity { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      deviceIdentityChannel.setMessageHandler(nil)
    }
    /// The largest texture edge this GPU will accept, in pixels (Phase 11 §3.1).
    ///
    /// The viewer needs this before it uploads a panorama, and it is the one
    /// number that decides whether the sphere renders or comes up **black**.
    /// Many mid-range tablet GPUs cap at 4096, and an 8192-wide upload against
    /// such a cap does not raise an error — it silently produces nothing, which
    /// looks exactly like a stitcher that emitted an empty image.
    ///
    /// Returns 0 when the platform cannot say, which the caller must read as
    /// "assume the conservative floor" rather than "no limit".
    let maxTextureSizeChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.maxTextureSize\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      maxTextureSizeChannel.setMessageHandler { _, reply in
        api.maxTextureSize { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      maxTextureSizeChannel.setMessageHandler(nil)
    }
    /// Total physical RAM in MB, for the memory-tier probe (arch §6.5).
    let totalPhysicalMemoryMbChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.totalPhysicalMemoryMb\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      totalPhysicalMemoryMbChannel.setMessageHandler { _, reply in
        api.totalPhysicalMemoryMb { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      totalPhysicalMemoryMbChannel.setMessageHandler(nil)
    }
    /// How much more memory *this process* may allocate, in MB, or `-1` where
    /// the platform will not say.
    ///
    /// Not the tier decision — that is [totalPhysicalMemoryMb], and it is total
    /// rather than available on purpose (Phase 10 §4): available fluctuates, and
    /// a tier that moves with it would make output resolution differ between two
    /// runs on the same device, which makes a bug report useless.
    ///
    /// This is the pre-flight check instead. iOS answers it with
    /// `os_proc_available_memory()`, and it matters there specifically because
    /// iOS kills a process on memory pressure with no recoverable signal — no
    /// `bad_alloc` to catch, no chance to drop a tier. So the only defence is to
    /// ask before starting, and to start one tier lower if the answer is smaller
    /// than the tier needs. Android returns `-1`: it has no per-process
    /// equivalent, and a real `OutOfMemoryError` there *is* catchable, so the
    /// retry path covers it.
    let availableProcessMemoryMbChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.availableProcessMemoryMb\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      availableProcessMemoryMbChannel.setMessageHandler { _, reply in
        api.availableProcessMemoryMb { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      availableProcessMemoryMbChannel.setMessageHandler(nil)
    }
    /// Battery charge as a whole percentage, or `-1` where the platform will not
    /// say.
    ///
    /// Phase 12 §3 asks for the drain **per station** to be measured and
    /// published, because a site walk is up to thirty of them and a tablet that
    /// cannot finish one is a feature nobody can use. It is a percentage rather
    /// than a mAh figure on purpose: percentage is what both platforms report
    /// without a vendor API, and the number that matters is "how many stations
    /// does this tablet have left", which is a percentage divided by a
    /// percentage.
    ///
    /// It is coarse — most devices report in steps of 1% and some in steps of 5%
    /// — so one station's drain is at or below the quantisation. The device matrix
    /// therefore measures across a *run* of stations and divides, which is also
    /// the number a site lead actually wants.
    let batteryPercentChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.batteryPercent\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      batteryPercentChannel.setMessageHandler { _, reply in
        api.batteryPercent { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      batteryPercentChannel.setMessageHandler(nil)
    }
    /// Closes the camera and releases every resource.
    let closeChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SphereCameraHostApi.close\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      closeChannel.setMessageHandler { _, reply in
        api.close { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      closeChannel.setMessageHandler(nil)
    }
  }
}

/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol SphereCameraFlutterApiProtocol {
  /// A preview frame is ready, stamped on the shared monotonic base. Phase 07
  /// pairs this with the pose stream.
  func onFrameAvailable(timestampUs timestampUsArg: Int64, completion: @escaping (Result<Void, PigeonError>) -> Void)
  /// Something went wrong outside a pending call — a disconnect, a HAL error,
  /// a dropped burst.
  func onError(code codeArg: String, message messageArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void)
  /// Thermal state changed (§5). At `serious` the UI warns and suggests
  /// stitching later; at `critical` a stitch is refused and told why.
  func onThermalStateChanged(state stateArg: PlatformThermalState, completion: @escaping (Result<Void, PigeonError>) -> Void)
  /// The session was interrupted or resumed — a phone call, another app, or
  /// split view on iPad, where a second app taking the camera is common
  /// (§7 pitfall 3).
  func onSessionInterrupted(interrupted interruptedArg: Bool, reason reasonArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void)
}
class SphereCameraFlutterApi: SphereCameraFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: MessagesPigeonCodec {
    return MessagesPigeonCodec.shared
  }
  /// A preview frame is ready, stamped on the shared monotonic base. Phase 07
  /// pairs this with the pose stream.
  func onFrameAvailable(timestampUs timestampUsArg: Int64, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onFrameAvailable\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([timestampUsArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
  /// Something went wrong outside a pending call — a disconnect, a HAL error,
  /// a dropped burst.
  func onError(code codeArg: String, message messageArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onError\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([codeArg, messageArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
  /// Thermal state changed (§5). At `serious` the UI warns and suggests
  /// stitching later; at `critical` a stitch is refused and told why.
  func onThermalStateChanged(state stateArg: PlatformThermalState, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onThermalStateChanged\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([stateArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
  /// The session was interrupted or resumed — a phone call, another app, or
  /// split view on iPad, where a second app taking the camera is common
  /// (§7 pitfall 3).
  func onSessionInterrupted(interrupted interruptedArg: Bool, reason reasonArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SphereCameraFlutterApi.onSessionInterrupted\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([interruptedArg, reasonArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
}
/// The AHRS half (Phase 07).
///
/// Deliberately a second API rather than more methods on
/// [SphereCameraHostApi]: the pose stream outlives any one camera session — §6
/// pitfall 5 says never reset the buffer between targets — and it must keep
/// running while the camera is closed and reopened. Two APIs make that
/// independence structural instead of a convention someone has to remember.
///
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol SpherePoseHostApi {
  /// What this device's motion hardware can do. Cheap, and callable before
  /// anything is started, because Phase 12 §1 refuses an unsupported device at
  /// the *feature entry point* — before the camera opens, before the user has
  /// walked to the first station.
  func poseCapabilities(completion: @escaping (Result<PoseCapabilities, Error>) -> Void)
  /// Starts the attitude stream at [samplingPeriodUs] (§4 asks for 10 000 µs,
  /// i.e. 100 Hz).
  ///
  /// Throws when the device is unsupported, so the refusal cannot be missed by
  /// a caller that forgot to check [poseCapabilities] first.
  func startPose(samplingPeriodUs: Int64, completion: @escaping (Result<PoseStreamInfo, Error>) -> Void)
  /// Stops the stream and releases the sensors.
  func stopPose(completion: @escaping (Result<Void, Error>) -> Void)
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class SpherePoseHostApiSetup {
  static var codec: FlutterStandardMessageCodec { MessagesPigeonCodec.shared }
  /// Sets up an instance of `SpherePoseHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: SpherePoseHostApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// What this device's motion hardware can do. Cheap, and callable before
    /// anything is started, because Phase 12 §1 refuses an unsupported device at
    /// the *feature entry point* — before the camera opens, before the user has
    /// walked to the first station.
    let poseCapabilitiesChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.poseCapabilities\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      poseCapabilitiesChannel.setMessageHandler { _, reply in
        api.poseCapabilities { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      poseCapabilitiesChannel.setMessageHandler(nil)
    }
    /// Starts the attitude stream at [samplingPeriodUs] (§4 asks for 10 000 µs,
    /// i.e. 100 Hz).
    ///
    /// Throws when the device is unsupported, so the refusal cannot be missed by
    /// a caller that forgot to check [poseCapabilities] first.
    let startPoseChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.startPose\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      startPoseChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let samplingPeriodUsArg = args[0] as! Int64
        api.startPose(samplingPeriodUs: samplingPeriodUsArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      startPoseChannel.setMessageHandler(nil)
    }
    /// Stops the stream and releases the sensors.
    let stopPoseChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sphere_view.SpherePoseHostApi.stopPose\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      stopPoseChannel.setMessageHandler { _, reply in
        api.stopPose { result in
          switch result {
          case .success:
            reply(wrapResult(nil))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      stopPoseChannel.setMessageHandler(nil)
    }
  }
}

/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol SpherePoseFlutterApiProtocol {
  /// One attitude sample, at the requested rate.
  ///
  /// One message per sample rather than a batch: the guidance reticle of Phase
  /// 09 is driven from the newest pose, and batching 100 Hz into 10 Hz packets
  /// would add up to 100 ms of visible lag to the aim gate for no benefit the
  /// interpolator needs — `PoseBuffer.at()` always looks *backwards* to a
  /// shutter that has already happened, so delivery latency costs it nothing
  /// and staleness costs the UI directly.
  func onPoseSample(sample sampleArg: PlatformPoseSample, completion: @escaping (Result<Void, PigeonError>) -> Void)
  /// The stream failed after starting — the sensor was unregistered by the OS,
  /// or motion updates stopped. Never silent (architecture §8).
  func onPoseError(code codeArg: String, message messageArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void)
}
class SpherePoseFlutterApi: SpherePoseFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: MessagesPigeonCodec {
    return MessagesPigeonCodec.shared
  }
  /// One attitude sample, at the requested rate.
  ///
  /// One message per sample rather than a batch: the guidance reticle of Phase
  /// 09 is driven from the newest pose, and batching 100 Hz into 10 Hz packets
  /// would add up to 100 ms of visible lag to the aim gate for no benefit the
  /// interpolator needs — `PoseBuffer.at()` always looks *backwards* to a
  /// shutter that has already happened, so delivery latency costs it nothing
  /// and staleness costs the UI directly.
  func onPoseSample(sample sampleArg: PlatformPoseSample, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SpherePoseFlutterApi.onPoseSample\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([sampleArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
  /// The stream failed after starting — the sensor was unregistered by the OS,
  /// or motion updates stopped. Never silent (architecture §8).
  func onPoseError(code codeArg: String, message messageArg: String, completion: @escaping (Result<Void, PigeonError>) -> Void) {
    let channelName: String = "dev.flutter.pigeon.sphere_view.SpherePoseFlutterApi.onPoseError\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([codeArg, messageArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(PigeonError(code: code, message: message, details: details)))
      } else {
        completion(.success(()))
      }
    }
  }
}
